# doubak-extension 功能规划

> 状态：v0.7（2026-07-31）。本文只定义**做什么**，不定义技术选型细节。
> **实现进度不在这里**——它在 [README](README.md) 的「现状」。
> 附录 A 来自前代实现 `its-my-data/doubak`（Go/Colly，已停止维护）。
> 附录 B 来自 `doufen-org/tofu`（豆伴，MV3 扩展，620 star，维护至 2025-03），**是目前最接近的同类实现，改变了本设计的若干前提**。
> **附录 C 来自一份真实旧档案的实测**（2022-12 至 2024-08，782 MB，7353 个文件）——这是本设计第一次拿真实豆瓣页面验证假设，也修正了附录 A、B 的若干处。有实测结论的地方，以附录 C 为准。
> C.10 之后的几节来自**对真实豆瓣的实跑**（2026-07），那一轮又修正了 C.7 与附录 A。

## 0. 职责边界

插件是整个项目里**唯一不可重来的一步**：它在用户自己的浏览器里把豆瓣页面原样存下来。抓错了、漏抓了、抓完账号被封了，都没有第二次机会。

**插件负责**
- 在用户已登录的浏览器里，按用户自己的 IP 和节奏抓取
- 把**原始字节 + 原始 headers** 写进 WARC，不做任何清洗
- 记录三件下游无法恢复的事：`spec_version`、`coverage`、`intent`
- 判定每个响应是否可信（`ok|blocked|challenge|login|gone|soft404`）
- 维护跨次抓取的**抓取存档信息**与断点**存档点**
- 导出成 bundle 目录

**插件不负责**
- 解析豆瓣语义（那是 parser 的事）。插件只做「为了推进抓取而必须做的结构抽取」——下一页链接、条目 URL、图片 URL，以及**用于水位线的时间值**（见 §3.3）。抽出来的东西只进 frontier 与 index，不构成 bundle 的数据模型。
- 语言判定、外部数据关联、去重合并、生成网页
- 任何服务端交互。验收标准：**把服务器关掉，插件依然能产出一份完整可用的本地档案。**

README 说「所有功能都能在插件里找到入口」——指 UI 层的入口（导出、跳转下游工具），不是把整条流水线塞进插件。

---

## 1. 已确定的决策

| # | 决策 | 影响 |
|---|---|---|
| D-1 | **每次抓取产出一个新 bundle**，绝不覆盖 | 见 §2 的隔离方案 |
| D-2 | **抓到一条写一条**，不攒批 | 见 §2.3 的落盘顺序 |
| D-3 | `capture_id` 用**单调递增序号**（bundle 内），`WARC-Record-ID` 仍用 `urn:uuid:` | 序号便于人读与排序；uuid 满足 WARC 标准与既有工具期望 |
| D-4 | **首发只做 Chromium** | 抓取核心不得依赖 Chromium 专有 API，保活层允许 |
| D-5 | **不做 JS 渲染抓取** | 全部走 fetch；原 F-04d 删除 |

另外两条从前代实现里学到的**反面教训**：

- **不要伪造 User-Agent。** 前代写死了一个 Chrome 109 的桌面 UA。在插件里浏览器自己的 UA 才是对的——伪造出来的 UA 与 TLS 指纹/其他请求头不一致，反而更容易被风控挑出来。
- **不要照抄它的请求头。** 前代有 `r.Headers.Set("Host", "https://www.douban.com/")`，把 Host 设成了一个 URL，是 bug。

**关于请求头的准确表述**（v0.3 修正）：原则是**不伪造身份**（UA 等一律用浏览器默认），但 **`Referer` 必须设对**——豆瓣的移动端 API 要求 Referer 指向对应页面，不设就取不到数据。而 `fetch()` 里 `Referer` 是禁止修改的 header，只能靠 `declarativeNetRequest` 规则改写（tofu 的做法见附录 B.4）。设一个真实浏览器本来就会发的 Referer 是**提高**保真度，不是降低。

---

## 2. bundle 隔离与写入

### 2.1 命名（保证任何两次抓取都不冲突）

```
bundle_id   = <UTC 紧凑时间戳>-<短随机>       例：20260728T101500Z-a3f9c1
目录          doubak-bundle-<bundle_id>/
段文件        data-<bundle_id>-00001.warc.gz      用户范围的页面与 API 响应
              assets-<bundle_id>-00001.warc.gz    用户上传的图
              catalog-<bundle_id>-00001.warc.gz   作品详情页与目录缩略图
索引          index-<bundle_id>.ndjson
清单          manifest.json
断点          checkpoint.json
```

段前缀表示**留存等级**，不是媒体类型：`data-` 与 `assets-` 是神圣的（源站没了就没了），`catalog-` 是缓存（源站还在就能重抓）。实测目录页占 90.3% 的体积，单独成段让「丢掉九成体积」变成一次 `rm catalog-*`。

段文件名**带 bundle_id**：即使用户把十次抓取的文件全倒进同一个目录，也不会互相覆盖。`bundle_id` 前缀是时间戳，天然可排序。

### 2.2 记录级唯一性（同一 bundle 内也不许互相覆盖）

- `capture_id = <bundle_id>#<seq>`，`seq` 从 1 单调递增，零填充。由构造保证全局唯一。
- `WARC-Record-ID = urn:uuid:<uuid>`，满足 WARC 对全局唯一的要求。
- `index.ndjson` 每行同时带 `capture_id` 与 `warc_record_id`，两个命名空间互为对照。
- **序号在写入前分配。** 崩溃只会留下空洞（某个 seq 没被使用），不会留下重复。空洞无害，重复会毁掉索引。
- WARC 元数据保持原味，doubak 专有字段全部在 `index.ndjson` 里——pywb / ReplayWeb.page 必须能直接打开。

### 2.3 落盘顺序（D-2）

每抓完一个响应，立刻按这个顺序写，中间任何一步崩溃都可恢复：

```
1. 分配 seq
2. 追加 WARC 记录到当前段（每条记录一个独立 gzip member）
3. 追加 index.ndjson 一行
4. 更新 frontier 状态 + checkpoint（IndexedDB）
```

顺序不能换。先写 index 后写 WARC 会留下**指向不存在记录的索引项**，那比孤儿记录难处理得多。

**崩溃恢复**：启动时检查当前段尾部——每条记录是独立 gzip member，撕裂的尾巴可检测。把段截断到最后一条完整记录，再把 index 截断到与之匹配，然后从 checkpoint 继续。

**段轮转**：超过大小上限即封段换新段。建议上限 **256 MB**（低配机器也处理得动，又不至于产生几千个文件）。每次轮转重写一次 `manifest.json`（它很小）。

---

## 3. 抓取边界模型（本次新增的核心）

### 3.1 为什么广播需要单独一套

前代实现的注释确认了：**HTML 广播页每页条数不固定**（"the number of broadcasts in each page somehow don't equal"），无法按条数推算。

**v0.4 修正——这条比原先想的严重得多**：豆瓣的计数器**在所有路线上都不可信**，不只是广播。原因是豆瓣有多套审查／屏蔽机制，计数**有时统计于审查之前、有时统计于审查之后**。所以「页面说 1234，我抓到 1234」推不出「档案完整」。

后果：**CLAUDE.md 里「可数列表用 `coverage.claimed_count` 对账」这条安全网不成立，已撤掉。**

于是所有路线只剩**一张**安全网：

| | 标记/想看列表 | 广播 |
|---|---|---|
| 有总数吗 | 有（页面与 API 都给） | API 给 `statuses_count` |
| 这个数可信吗 | **不可信**（审查前后不一） | **不可信**（同上） |
| 安全网 | **连续性证明 + 枚举方式** | **连续性证明 + 时间边界** |
| 终止条件 | 走到水位线 / 停滞检测 | 走到时间下界 / 停滞检测 |

数字照记不误（事后不可恢复，且有取证价值——`claimed > captured` 可能意味着豆瓣的计数器知道一些它不肯给你看的条目），但它是**线索，不是判决**。spec 里因此刻意**不提供** `completeness` / `reconciled` 字段：不存在的字段无法被误用。

### 3.2 两个边界

每条路线在每次抓取中都有一对边界：

- **下界 `floor_time`（往回抓到哪儿为止）**
  - 首次抓取：无下界，一直抓到最早（或由用户指定一个起始日期）
  - 增量抓取：上次成功抓取留下的 `high_water_time`
  - 比较用**闭区间 `<=`**，宁可重复不可遗漏
- **上界 `high_water_time`（本次抓到的最新一条的时间）**
  - 本次跑完后写入抓取存档信息，成为下次的下界

### 3.3 水位线只能在干净完成时推进（最重要的不变量）

> **只有当某条路线从最新一条**连续无缺口**地走到了 `floor_time`，它的 `high_water_time` 才允许推进。**

中途暂停、被风控打断、用户放弃——一律**不推进**水位线。已经抓到的数据照样留在 WARC 里（不浪费），但下次仍从旧下界重走。重复是免费的，空洞是永久且不可检测的。

会话内的中断由 **checkpoint** 负责续抓，不需要重走；只有整场放弃才会重走。

**v0.3 已解决**：广播列表页每条都带完整绝对时间，在 `title` 属性里（可见文本才是省略形式）：

```html
<span class="created_at" title="2026-07-26 12:34:00">
  <a href="https://www.douban.com/people/82160871/status/9351468114/?_spm_id=...">7月26日</a>
</span>
```

所以水位线可以直接从 HTML 列表页取，不必依赖 API，也不必逐条进详情页。增量机制立得住。

仍要注意两点：

- **时区未标注。** `2026-07-26 12:34:00` 没有偏移量，几乎肯定是服务端的北京时间（UTC+8），但这是**推断**。插件必须原样保留字符串，另存一个显式带偏移的解析结果，并把「假定时区」记进 spec 字段——绝不静默转换。用户在海外时区跑抓取时，这一条直接决定水位线会不会偏 8 小时。
- **URL 带跟踪参数**（`?_spm_id=...&_dtcc=1`）。同一条广播在不同页面出现时参数不同，会让去重失效，也会把跟踪参数写进档案。对策见 §3.7。

**v0.4 实测复核**：真实档案里 `class="created_at" title="2024-05-12 14:43:19"` 确实每条都有、精确到秒（附录 C.1）。此外广播条目还带 `data-sid`（去重锚点）与 `data-uid`（作者，用于第三方标注），比原设想的更好用。

### 3.3b 增量抓取：链的设计（v0.5 定，spec §5.5）

水位线算出来了、写进 manifest 了，但**没有代码把它读回来当下界**——`floors` 至今
只在调试用的小范围试跑里传。这一节是把它接上之前定下的设计。

#### 下界按**路线**选，不按档案选

`advanced` 是逐路线的。某条线有缺口 → `advanced: false` → 它**不提供下界**，下次
从头重走；同一份档案里其他线照样可以提供。

真实例子（档案 `786e5c`）：广播 `advanced: true`（能当基准），作品详情页
`有 1 处缺口 / advanced: false`（下次重抓）。

由此必然：某条线的下界 = **最近一份「这条线 `advanced: true`」的档案**的
`high_water_time`，要沿链往回找。所以同一次抓取里不同路线的下界可能来自不同档案，
顶层的 `previous_bundle_id` 表达不了——每条路线要记 `floor_from_bundle_id`
（spec §5.5.1）。

#### 删掉基准不会「弄坏」增量档案，只会让链上出现一个**看得见**的洞

用户会删档案，会只把一部分搬到另一台机器。所以链必须是**可核对的声明**，不是
运行时依赖：按路线核对 `floor_time == 上一份的 high_water_time` 且那一份在场，
缺一环就报一处洞（spec §5.5.3）。

**曾经考虑过复制基准，否决了**：作品详情页占档案九成体积，复制等于每次增量翻倍；
更要命的是，**同一个 URL 在不同时间的多次捕获本来就是有价值的数据**（评分变了、
短评改了），复制会把「两个版本」混淆成「同一份数据的两份拷贝」——恰好抹掉我们
想留的那个差异。

#### 「已经抓过没有」按**账号**问，不按链问

这两个问题不是一回事，混起来的代价很贵：

| 问题 | 用什么回答 |
|---|---|
| 「从今天往回一直到 X 有没有断」 | **链**（`previous_bundle_id` 串起来的那条） |
| 「这一页我是不是已经有了」 | **这个账号名下的全部档案** |

作品详情页没有时间序，链对它毫无意义。而按链算的后果在真实使用里立刻就出来了：
`previous_bundle_id` 为 null 的档案各自成链，于是「最新那条链」常常只有一份——
那一份要是刚跑了一小段的增量（只抓到十几个详情页），**此前几千个就全都不认识了**，
下一次增量把它们重抓一遍。用户看到的是「我只加了一本想读的书，它却在抓游戏」。

判据仍然是同账号同用户名（改过名的不算——那时 URL 里的用户名对不上）。

#### 没有时间锚的路线不适用增量

作品详情页不分页、条目之间没有时间序，`advanced` 恒为 false。这不是缺陷，是如实
描述（spec §5.5.5）。它的增量策略是**只抓本次列表里新出现的条目**，另给一个显式的
「刷新作品详情页」按钮——把「要不要重抓」变成用户的决定，而不是默认行为。这与
`acceptLeafGaps` 是同一个模式。

#### 界面：链必须看得见

增量之后，**单份档案的 `captured` 不再有完整性含义**（可能只有 3 条新的）。两个
页面都要显示链：

**覆盖率页**改成两个视角，默认「合起来」：

```
合起来 · 3 份档案                          [ 这一份 ]

广播          2014-01-10 ─────────── 2026-08-15   连续 · 跨 3 份
电影 · 看过    2014-01-10 ─────────── 2026-08-15   连续 · 跨 2 份
作品详情页     —                                   不适用（无时间锚）
```

主角是**连续区间**，不是数字。**刻意不去算「union captured」再跟 claimed 比**：
边界是闭区间去重、必然重叠，算出来的数只会误导；而这个项目的论点本来就是
「计数不能证明完整性，连续性才能」。claimed 仍显示（取最新一次观测），只作线索。

**档案页**：列表里标「全量 / 增量（基于 xxx）」；打开时顶部显示整条链；每条捕获标
**新增 / 又抓了一次**（按 `url_key` 在链上更早的档案里出现过没有——只读 index，
不解压）；作品详情页按 `url_key` 聚合出**版本历史**（「3 个版本：07-31 / 08-15 /
09-02」）——那正是「有意保留不同版本」的兑现处，也是 canonical 的 revision 模型的原料。

**删除**：警告「这份是 N 份增量的基准」（警告，不阻止）；存储页显示引用计数；导出
提供「这一份」/「整条链」。

### 3.3c 一条分页路线到底怎么算走完（2026-08-15 对着真实档案理清）

**先说结论：不是两种，是五种，而且哪几种能用取决于路线的配置。** 这一节存在的理由
是：原来「怎么收尾」这件事散在 `_enqueueNextPage` 的五个 `return` 里，谁也说不清某
条路线到底靠什么停下来——而停早了是静默截断，停晚了是白发请求。

| 判据 | 什么意思 | 哪些路线拿得到 |
|---|---|---|
| `reached_floor` | 走到上次的水位线了 | 全部（**仅增量时**） |
| `stalled` | 连续 3 页没有新条目，或**连续 2 页一条都没有** | 全部（兜底） |
| `empty_page` | 这一页一条都没有，而步长就是本页条数（`step: 'items'`）→ 步长为 0 | 日记 / 评论 / 豆列索引 |
| `last_page` | **这一页自己说**「第 K 页 / 共 N 页」且 K ≥ N | 豆列详情页 |
| `no_next_page` | 派生路线算不出下一页的地址 | 豆列详情页 |

不分页的路线（个人主页、5 个分类入口、作品详情页、长文正文、各类图片）不在这张表
里：它们抓到那一页就算走完，判据是「有 `entryUrl` 且没有 `pagination`」，或者对派生
集合而言是队列空了。

#### 一条路线底下可以装着好几串互不相干的分页

`doulist.item` 是第一条这样的路线：一条路线底下有 6 份豆列，每份自己从 `start=0`
翻到头。而**停滞检测的计数器是按路线数的**，于是它会把不同串的空页数到一起。

实测这一趟（bundle `20260814T223824Z-4b82f3`）——frontier 是**广度优先**的，抓取顺序
是六份豆列的第 1 页、再六份的第 2 页：

```
162349128 空 · 54002196 24条 · 45473911 20条
42193343 空 · 3913537 空 ← 连续第 2 个空页，判停滞
```

`3913537` 因此被停在第 2 页、不再入队第 3 页——**它是被别的豆列的空页停掉的**。那一趟
侥幸没丢东西（那几份本来就翻到头了），但换成一份第 3 页还有内容的豆列，就是静默截断，
而这条路线连声称数都没有，漏了没有任何信号。

所以：**有 `nextPageUrl` 的路线不看路线级的停滞计数器**，改用两个判「这一串」的判据
——`last_page`（页面自己说）与「本页一条都没有」。广播与标记列表一条路线就是一串，
计数器对它们仍然是对的判据，不受影响。

这也是「一条路线的进度状态」这个模型第一次不够用。再出现第二条这样的路线时，该做的
多半是把停滞检测下沉到「串」这一层，而不是继续加特例。

#### `data-total-page` 不是一样东西 —— 这是这一节最要紧的一条

「读翻页器就能少发请求」听起来该全站通用。实测 8 条分页路线、全部真实档案之后，
它压根不是一个统一的信号：

| 路线 | 有 `div.paginator` | `data-total-page` 见过的值 |
|---|---|---|
| 广播 | 184/184 页都有 | **`9223372036854775807`** |
| 标记·影视 | 183/183 | 89, 90, 50, 51, 2 |
| 标记·游戏 | 84/84 | 21, 18, 3 |
| 标记·书 | **53/53 有翻页器，却一次都没有这个属性** | 从没有过 |
| 标记·音乐 | 20/90 | 从没有过 |
| 标记·舞台剧 | 0/30 | 从没有过 |
| 日记 / 评论 / 豆列索引 | 0 | 从没有过 |
| **豆列详情页** | 8/18（多页的那几份每页都有） | 2, 3 —— 与实际内容页数**完全吻合** |

三件事：

**① 广播那个数是 `Long.MAX_VALUE`。** 豆瓣在说「页数无限」。照着它判 `K ≥ N` 永远
不成立，所以即使误用也不会截断——但它证明了**这个属性可以是个哨兵值而不是计数**。
一条「全站统一读翻页器」的规则会在这里读到一个纯粹的谎话。

**② 同一条路线，不同媒介行为不同。** 书有翻页器却没有这个属性，舞台剧连翻页器都
没有。这已经是这个项目第三次撞上「一套选择器跨不过所有媒介」（前两次：游戏的评分与
短评、音乐与舞台剧的短评容器）。

**③ 标记列表上的那个数，本质上是豆瓣的声称数除以每页条数。** 而规范 §2 明确禁止拿
声称数去**授予**完整性——豆瓣的计数有时在审查层之前算、有时在之后算（实测 游戏/玩过
声称 293、渲染 288）。拿它当终止条件，就是用一个不可信的计数去决定「不用再往下读
了」，方向恰好是**会截断**的那个。

所以 `last_page` **只给量准了的那条路线**（豆列详情页：2 和 3，与实际内容页数逐份
吻合），判据挂在该路线的判定档案上（`classifier.js` 的 `paginator`），不是全局开关。
而且只往「更早收手」的方向用：没有翻页器就退回原来的走法，行为与从前一致。

### 3.4 抓取存档信息（需要写进 spec）

跨次抓取的状态，**每条路线一份**：

```
route_key            路线标识（含 intent）
high_water_time      本路线已确认覆盖到的最新时间
high_water_ids       该时间点上的条目 ID 集合（同秒多条时用于去重）
completed_at         达成这个水位线的那次抓取的完成时间
bundle_id            达成它的那个 bundle
contiguous_from      连续性证明覆盖的起点（广播专用）
claimed_count        当时页面声称的总数（可数路线专用）
captured_count       当时实抓数量
```

存在两处：写进 bundle 的 `manifest.json`（档案自带、可迁移），也留在 IndexedDB（下次抓取直接读）。**导入旧 bundle 即可恢复水位线**——换机器、重装插件都不丢。

### 3.5 抓取存档点 / checkpoint（需要写进 spec）

断点续抓用，**每次抓取一份**：

```
bundle_id            所属 bundle
paused_at            暂停时间
pause_reason         session_expired | challenge | blocked | user_paused | quota | crash
frontier_digest      frontier 状态摘要（校验用）
last_capture_id      最后一条成功写入的记录
per_route_cursor     每条路线的当前游标（URL / start 值 / 已达时间）
resume_policy        恢复后的节奏（降速后不自动恢复原速）
```

抓取期间存 IndexedDB；**导出时一并写进 bundle**（即使是未完成的 bundle），这样一份导出的半成品档案也能被拿到别处续抓。

### 3.6 两个抓取面：HTML 页面 vs 移动端 API（v0.3 新增，最重要的一处改动）

tofu 几乎**不抓 HTML**，它走的是豆瓣移动端的 Rexxar JSON API（附录 B.1）。这条路我们原本完全没考虑，但它在几个关键点上明显更优：

| | HTML 页面 | Rexxar API |
|---|---|---|
| 解析稳定性 | 15 年的 markup 变迁，选择器易碎 | 结构化 JSON，字段稳定得多 |
| 翻页 | 页码/`start` 偏移，头部插入会错位 | **游标 `max_id`（按值）**，天然免疫头部插入 |
| 总数 | 要从文案里正则抠数字 | 直接给 `total` / `statuses_count` |
| 归档保真 | **就是用户当年看到的东西** | 私有接口，随时可能变或消失 |
| 长文本 | 完整 | **140 字截断**，需逐条补全 |

两边各有不可替代之处，所以**不是二选一**：

> **API 负责「枚举」，HTML 负责「保真」。**
> 用 API 拿到可靠的条目清单、游标、总数——它驱动 frontier 与覆盖率核对；
> 用 HTML 留下用户当年真正看到的页面——它才是归档物。
> **两者都完整写进 WARC。**（都是 HTTP 响应，WARC 存得下，pywb 也放得进去。）

由此产生一个 **`index.ndjson` 的新必填字段**：

```
surface : "html" | "api"
```

parser 必须知道每条记录来自哪个面，否则同一条广播的两份表示会被当成两次修订。这个字段属于「丢了就完蛋」的那一类，**必须进 spec 的第一版**。

推论：附录 A 的 HTML 选择器仍然有用（保真面 + 分类器锚点），但**不该拿来做主力枚举**。原设计里「靠 HTML 选择器数条目」的地方要改成「靠 API 计数」。

**风险**：Rexxar 是未公开的私有接口，随时可能变。所以 HTML 面不能砍——它是 API 失效时的退路，也是归档承诺本身。

### 3.7 URL 原样保留，另存归一化键

CLAUDE.md 的铁律是「捕获时不做归一化」，这条不动摇：`WARC-Target-URI` 一律写**请求时的原始 URL**，跟踪参数照留（它们本来就是当时那个页面的一部分）。

但去重不能用原始 URL，否则 `_spm_id` 一变就当成新页面。所以 `index.ndjson` 里**额外**存一个 `url_key`：剥掉已知跟踪参数（`_spm_id`、`_dtcc` 等）、统一大小写与尾斜杠后的结果。

原始 URL 是事实，`url_key` 是索引——两者并存，不互相覆盖。剥离规则本身要进 spec 并带版本号，将来发现新的跟踪参数可以重算 `url_key`，而原始 URL 永远不动。

---

## 4. 功能清单

优先级：**P0** = 「装上插件就能拿到完整档案」不成立就缺不得；**P1** = 决定档案可不可信；**P2** = 之后再说。

> **这张表是设计意图，不是进度表。** 实现到哪一步看 [README](README.md) 的「现状」——
> 那里是唯一的状态来源。
>
> 表里出现 ✅ / ⚠ / 日期标注的那些行，记的**不是「做完了」，而是「被真实数据修正过」**：
> 某条设计撞上了真实的豆瓣或真实的浏览器，于是连同证据一起写回原处。把这些留在设计文档
> 里，是因为**没有证据的设计条款迟早会被人凭直觉推翻**——而这里几乎每一条踩过坑的规则，
> 直觉给出的答案都是错的。

### A. 身份与会话

| ID | 功能 | 优先级 | 说明 |
|---|---|---|---|
| F-01a | 识别当前登录账号 | P0 | 从导航栏取用户 ID 与昵称，作为 bundle 归属。未登录不让开始。 |
| F-01b | 会话失效检测 | P0 | 会话过期是**停止条件**，不是可重试错误。立即停机并写 checkpoint。**三条独立理由**（v0.6 补充第一条）：① 未登录的请求频率上限更低——掉了登录还按原节奏抓，等于拿一个更严的配额去撞，这正是限流升级成封号的路径，所以继续抓这件事**本身**就变危险了；② 未登录看到的是公开视图，私密条目不在里面；③ 混进别人的账号不可逆。 |
| F-01e | 开抓前的身份确认（preflight） | P0 | 必须先确认已登录、并取到**数字用户 ID**。实测：导航栏只给昵称与用户名，数字 ID 只在部分页面（广播条目的 `data-uid`）和个人主页上有——而它是档案的归属主键。所以身份确认要单独一步，之后每页只做廉价复核。 |
| F-01c | 账号切换检测 | P0 | 抓取中用户在别的标签页换了账号 → 立即停止。一个 bundle 只属于一个账号，混进别人的数据是不可逆污染。 |
| F-01d | 抓取前风险告知 | P0 | 说明：请求全部来自你自己的浏览器和 IP；被风控会暂停等你处理；cookie 不发往任何地方。 |

### B. 覆盖率基线

| ID | 功能 | 优先级 | 说明 |
|---|---|---|---|
| F-02a | 记录声明数量 | P0 | 记录豆瓣**自己声称**的数量。事后无法补，所以必须记；但它**不是**完整性依据（§3.1、附录 C.2）。<br><br>**来源是列表页自己，不是个人主页**（v0.5 定；早先的表述是「抓各分类入口页」，那是错的，也是一个真实的误解来源）。每一张列表页上都有 `<h1>我看过的影视(1333)</h1>`，逐页复读还能发现抓取过程中总数变了。<br><br>这个区别要紧，因为**个人主页是用户可自定义的**：显示哪些模块、什么顺序都能改，有人根本没有「影视」区块。如果声称数量真的从那里取，那类用户的覆盖率对账就整片是空的。实现上从来没依赖过它——15 条标记列表的抽取拿一份真实档案逐条核对过，三种写法都覆盖（紧凑 `我看过的影视(1333)`、带用户名前缀 `MewX玩过的游戏(306)`、多行缩进的舞台剧）。同理，**路线是按模板拼 URL 的**，不靠解析个人主页去发现分类，所以少一个区块也不会少抓一条线。 |
| F-02b | 声明页本身入 WARC | P0 | 不能只存 manifest 里的一个数字，来源页面要留证。 |
| F-02c | 记录差值，**不下完整性结论** | P1 | 跑完后把 `claimed` 与 `captured` 的差值写进 manifest。UI 里必须说清楚它**不代表**档案缺了东西——豆瓣的计数器审查前后不一。把它当完整性指标显示，是在给用户一个假的安心。 |
| F-02d | 连续性证明（所有路线，不只广播） | P1 | 这是**唯一**的完整性证据。用相邻页的**重叠证据**串成从最新到下界的不断链。缺口显式记录，不静默。原先只给广播用，现在计数对账没了，标记类列表也得靠它。 |
| F-02e | 游戏/舞台剧的总数在列表页上 | P1 | 这两类没有独立入口页，总数在列表页的 tab 里，取数逻辑与其他分类不同（附录 A）。 |

### C. 路线规划与 frontier

| ID | 功能 | 优先级 | 说明 |
|---|---|---|---|
| F-03a | 路线注册表 | P0 | 每条路线声明：URL 模板、`intent`、翻页方向、步长、终止条件、安全网类型、结构锚点。新增路线 = 加一条注册项，不改抓取引擎。 |
| F-03b | `intent` 随请求下发 | P0 | 由规划器指定并一路带到 `index.ndjson`。列表第 7 页事后无法区分「看过」还是「想看」——**丢了就是永久丢了**。 |
| F-03c | 持久化 frontier | P0 | IndexedDB 队列，状态机见 §5。每抓完一页就落盘。**恢复时状态原样还原**——早先一律按「新条目」重建（pending、attempts 归零），于是 checkpoint 里写下的 `failed` 被静默丢弃，等于每次恢复都偷偷给一次新的重试预算，而崩溃恢复每 30 秒就可能发生一次。 |
| F-03m | **失败只堵有序路线** | P0（2026-07-30 修） | 有序路线（分页列表）上的失败必须堵住整条线：跳过抓不下来的第 7 页去抓第 8 页，就再也不能声称「第 7 页以上全都抓到了」，而水位线正建立在那句话上。**但叶子条目之间没有先后关系**——作品详情页是一个集合，一个电影页与另外 1332 个无关。原来一律连带，后果是**一页失败葬送九成档案**（作品详情页占真实档案 90.3% 体积）。`ordered` 由「有没有 pagination」推导。 |
| F-03n | **有未解决的失败就不许标 complete** | P0（2026-07-30 修） | 失败不调用 `frontier.stop()`，所以 `stoppedBy` 是 null，于是「没有可跑的了」曾被上层当成干净跑完——档案被静默标成 `complete`，而 `crawl_state` 与 `coverage` **双双为空**，那一页的缺失完全不可检测。现在：每个失败都记一处 `fetch_failed` 缺口；有序失败**只能重试**；叶子失败可以由**用户**确认「就这样收尾」（规范 bundle/v1 §5.0 明确允许带缺口 complete，前提是缺口如实记录且 `advanced=false`）。**重试永不自动**——反复撞同一面墙，如果那面墙是风控，代价是账号；有测试钉着心跳与恢复路径里都没有重试调用。 |
| F-03e-fix | **优先级要真的生效** | P0（2026-07-30 修） | `priority` 原来只影响路线定义的排序，而 `next()` 按**入队顺序**取。翻页会把后续页面追加到队尾——广播第 2 页排在所有标记列表种子之后，于是广播与列表**交错抓**。交错的后果是一次中断让所有路线都半途而废，正好抹掉 F-03d 的全部意义。现在 `next()` 严格按优先级取（同优先级内先进先出，分页必须按页序）。 |
| F-03k-fix | **作品详情页的 URL 从列表页派生** | P0（2026-07-30 补） | `interest.item` 此前**压根跑不起来**：没有 `entryUrl`（不入种子），也没有任何代码把作品 URL 放进队列——一条占档案九成体积的路线，从来没抓过一个页面。现在列表页抓完即抽出链接入队（`extractSubjectLinks`，五种媒介各自的 URL 形态；拿旧档案 400 个列表页 5805 条链接验过，每页唯一链接数等于槽位数）。门控**照样入队、由 frontier 挡住**，而不是「门没开就丢掉」——丢掉的话一旦列表页先抓完，这些 URL 就再也不会被发现。它原来还带着一个错误的 `pagination`（作品页不分页），那让 `ordered` 推导把它判成有序，一个作品页失败会连带堵死其余的。 |
| F-03d | 按不可替代性排序 | P0 | **广播 → 自己的长文（日记/影评书评/读书笔记）→ 用户上传图片/相册 → 标记与想看列表 → 豆邮 → 豆列/社交图谱 → 作品详情页（目录数据，最后）**。中途被打断时，先跑完的一定是最难补的。作品详情页排在最末且受 F-03k 门控。 |
| F-03k | 路线前置依赖 | P0 | 路线可声明前置条件，未满足则不启动。目前唯一的硬依赖：**`interest.item`（作品详情页）必须等 `broadcast.timeline` 的 `advanced=true`**。这不是调度优化，是**防止拿最不可替代的东西去换最可替代的东西**——广播不可编辑、可静默删除、时间最敏感；目录页永远能回头再抓。做成通用机制而非写死的 if，是因为将来还会有别的依赖。 |
| F-03j | 豆邮（私信）单独对待 | P1 | tofu 有这条路线，我们原本漏了。豆邮是**用户创作内容里最不可替代的一类**（无任何公开副本），同时也是**隐私最敏感的一类**（内含对方的话）。必须：默认不参与任何发布路径（F-12a 之上再加一层），在 UI 里单独开关并明确告知。它还走旧版 `www` 接口而非 Rexxar（附录 B.3）。 |
| F-03e | 逆插入方向翻页 | P0 | 豆瓣列表都是头部插入，一律**新 → 旧**。抓取期间的新增会把条目推向尚未读到的页，产生重复（免费修复）而不是静默遗漏（不可检测、永久）。 |
| F-03f | 时间边界与水位线 | P0 | §3.2 / §3.3。存时间**值**不存 ID/页码，闭区间比较，跨会话按内容重新定位，绝不相信保存的页码。 |
| F-03g | 停滞检测终止 | P0 | 终止条件是「连续 N 页无进展」，不是「本页无新条目」，更**不是「本页条目数少于槽位」**。**v0.4 实测**：游戏列表第 7、14、17 页只渲染 14、14、13 条（槽位 15）——被审查抑制的条目留下的**中段空洞**。前代的「短页即末页」会在这里把列表拦腰截断。另：**不得用声称数量决定翻多少页**，前代 `for start < claimed_count` 在 `claimed < 实际` 时会静默截断整份列表。 |
| F-03h | 失败页不跳过 | P0 | 失败页保持未完成、阻塞该路线推进。跳过就破坏「这条线以上全部已抓」这个不变量。 |
| F-03i | 抓取范围选择 | P1 | 勾选路线族；广播可设起始日期（首次抓取用）。 |

### D. 抓取引擎

| ID | 功能 | 优先级 | 说明 |
|---|---|---|---|
| F-04a | 带会话取页 | P0 | 复用浏览器现有 cookie，**不改任何默认请求头**（见 §1）。同域并发恒为 1。 |
| F-04i | **全局互斥：同一时刻只跑一件事** | **P0**（v0.5 补） | 节奏闸门是**按活动**建的（一次抓取一个、身份确认一个、演练一个），所以两件活动同时跑时各自都遵守「1 秒一个」，合起来豆瓣看到的却是 **2 秒 3 个**。触发路径都很平常：抓取中去点小范围试跑、两个面板标签页各点一次开始、心跳在上一段没跑完时又来一次（popup 曾经是这里的头号来源，现在它没了——见 F-11a）。用 `Exclusive`（`src/crawl/exclusive.js`）圈住所有会发请求或会动抓取状态的操作，**被占用时立刻拒绝而不排队**——静默延后启动比直接说「已经有一个在跑」糟糕得多。`pause` / `status` 刻意不加锁，否则暂停会在一段 22 秒的批次期间失灵。身份确认与开工合并成一个临界区，否则两次点击会各自发出一次多余的身份确认请求。 |
| F-04b | 节奏控制 | P0 | **间隔从上一次请求结束算起**（不是从开始）——从开始算的话响应越慢我们催得越紧，而响应变慢恰恰是对方限流的信号，方向正好反了。默认 **1 秒 + ±30% 抖动**，与前代同速：核对源码确认前代的三处 `time.Sleep` 本来就是 finish-to-start，且它在同一账号上跑了 20 批次横跨两年未被封。我们在同速之上多三重它没有的保护：抖动、只增不减的退避、软封锁停机等人。跳转的**每一跳都过闸门**。 |
| F-04c | 尽可能原样保存 + **如实标注保真度** | P0 | 见下方「关于『原始字节』的一个硬限制」。不解压、不转码、不规范化的原则不变，但 `fetch()` 拿不到真正的原始字节，这一点必须在 spec 里显式承认，不能假装做到了。 |
| F-04e | 图片队列 | P1 | 独立低优先级队列。抓：用户上传内容（相册、正文内嵌）、列表页目录缩略图。跳过：CSS / JS / 字体 / 他人头像 / 大图海报。**不抓图片正是「备份必须联网才能看」的病根。** |
| F-04e-1 | **作品封面图**（2026-08-01 落地，第一批真的图片） | P1 | 在此之前，四份真实档案 **3797 条捕获全是 `text/html`，一张图都没有**——打开任何一页，封面全是指向 `doubanio.com` 的 URL。<br><br>做法是一条独立路线 `asset.subject_cover`：作品详情页判定为 `ok` 之后，从中抽出封面 URL 入队。`kind: catalog`（判据是**谁上传的**，不是长什么样——封面是目录数据，跟着详情页一起可整批丢），`surface: asset`（规范 bundle/1.1 新增；标成 `html` 会让下游按页面解析二进制）。<br><br>三处不能想当然：① **抽哪一张**——一页上有 20~40 个 doubanio 图片 URL，绝大多数是推荐区的别的作品、评论者头像、界面雪碧图。判据是**位置**（`#mainpic`，游戏页没有它，退到 `class="pic"`），不是「长得像不像封面」。退回「页面上第一个 doubanio 图片」会让 594 个游戏各存一份同样的 `new_menu.gif`——那正是游戏页上的第一张图。② **图片响应的判定**——没有结构锚点可用，收窄成「`Content-Type` 是 `image/*` 且非空」；**请求图片却收到 `text/html` 绝不判 `ok`**，豆瓣以 200 送封锁页是既有事实，图片请求没有理由被豁免。③ **不对图片做会话复核**——复核是在响应里找导航栏的用户 ID，而图片的字节里什么都可能有（EXIF、内嵌缩略图），一张图的元数据足以把整场抓取判成「账号被换掉」而停机。<br><br>实测：2916 个作品详情页抽到 2909 张封面，其余 7 个是豆瓣自己显示占位图（这个作品**本来就没有海报**），0 个抽取失败。两种「抽不到」必须分开记——混成一句，那 7 条正常情况就成了天天出现的噪音，真正的改版信号会淹死在里面。<br><br>**尚未做：用户自己上传的图**（相册、广播与日记正文内嵌）。那才是不可替代的那部分，该进 `assets-*`；它们藏在一段 `<script>` 里的 JSON 中（`photos = [{"image": {"large": {"url": ...}}}]`），不是 `<img src>`，需要另一个抽取器。实测一份真实档案的广播里有 **113 张**这样的图。 |
| F-10i | **崩溃恢复必须把「派生出来的活」补回来** | P0（2026-08-01 实测发现） | 捕获每页落盘、checkpoint 每批（25 条）才落，中间那个窗口里页面已经进了 index，而它派生的下一页链接 / 作品链接 / 封面图只活在内存队列里。worker 一死就没了，而恢复时 index 会把这张页面标成「抓过了」→ 不重取 → 派生永远不再发生 → **没有任何地方记下这件事**。<br><br>实测（6 部剧的小档案，批大小 3，模拟中途被杀）：不中断是「详情页 6 / 封面 6」，被杀过是「详情页 6 / **封面 2**」——4 个作品有页无图，静默且永久。<br><br>`capture_id` 序号单调递增，所以「checkpoint 之后写下了哪些」可以**精确算出来**：把它们重新入队，派生自然重来一遍，不需要新机制。代价是每次崩溃多至多一批的重复请求——**重复是免费的，空洞是永久的**。<br><br>两处配套：① `checkpoint.last_capture_id` 这个字段一直存在但**从来没人填**，恒为 null；不填就等于「全都没见过」，一恢复会把能派生的页面统统重抓，几百个多余请求。② 要重抓的那几条**不能**进 `markCaptured` 的去重集合，否则它们再也入不了队，而且失效得悄无声息（`enqueue` 返回 false 没人看）。<br><br>重抓有上限（100 条）且触及上限必须报出来：连着崩几次时待重抓的会堆积，不限量地重抓正是撞上风控的样子，而风控的代价是账号。规范 §7.2。 |
| F-04m | **图片必须带 Referer；418 是拒绝不是故障** | P0（2026-08-02 实测发现） | 抓封面图连续收到 **123 个 418**，`content-type: image/jpeg`、`content-length: 13`。两个独立的毛病：<br><br>**① 豆瓣图片域有防盗链。** 不带 `Referer` 一律 418（直接在地址栏打开图片 URL 也一样，所以与「是不是扩展」无关）。而 `Referer` 是 `fetch()` 的禁改头，只能靠 DNR 的 `modifyHeaders`。`transport.js` 一直按「发 `X-Override-Referer`，由 DNR 规则改写」来写——**而 manifest 里从来没有过 DNR 权限，也从来没有过规则**，整套机制自始至终是空的。之前抓的全是 douban.com 页面（不查防盗链），所以一直没暴露。<br><br>修法是一条会话规则（`crawl/referer-rule.js`）：`tabIds: [-1]` 限定为扩展自己发的请求，统一设 `Referer: https://www.douban.com/`，并顺手删掉内部标记头。静态值够用——防盗链看的是来源站点，不是具体哪一页；DNR 也做不到把一个头的值搬到另一个头上。权限用 `declarativeNetRequestWithHostAccess` 而不是 `declarativeNetRequest`：能力限死在已有 host_permissions 的站点上，避开「屏蔽任意网页上的内容」那句警告。<br><br>**② 418 不在任何一条判定分支里**，于是一路走到「判不出来」——记一次失败，然后若无其事地去抓下一张。豆瓣说了 123 次「不」，我们一次都没听懂，还打算再说 2900 次。**这正是「在软封锁上重试 = 把限流升级成封号」的样子。** 改判 `blocked`（停下来等人 + 降速）。注意 13 字节的响应还标着 `image/jpeg`，所以光看 Content-Type 认不出来，必须看状态码。 |
| F-04n | **网络断了要早早停手** | P0（2026-08-02 实测发现） | 电脑闲置、网络断开，一段推进跑了 **780 秒**。算得出来：一批 25 条 × （30 秒超时 + 1 秒间隔）≈ 775 秒，而预算（22 秒）**只在批与批之间检查**，拦不住进行中的那一批。期间心跳每 30 秒来一次、每次都只能说「上一段还在跑」——用户看到二十来行「未恢复」，像是彻底卡死。<br><br>网络断了是**全局状况**，不是某一条 URL 的问题：连着几条都连不上，就没有理由再试下一条。新增停机原因 `network_down`，`autoResume: true` 且 `userVisible: false`——用户什么都不用做，等着就好，为此弹通知只会训练他忽略通知。<br><br>阈值必须**大于单个条目的重试预算**（`1 + MAX_NETWORK_RETRIES` = 3），否则一个坏 URL 自己就能把整场抓取判成「网络断了」——把「这一页有问题」误读成「全世界都没了」。取两个条目的预算（6）：这是「不止一个 URL 出问题」所需的最小值。 |
| F-04o | **「上一段还在跑」不是错误** | P1（2026-08-02） | service worker 约 30 秒被杀一次，而 offscreen 活得久得多。新起的 worker 内存全空、以为没人在跑，就来叫一次恢复——互斥锁把它挡了回来，**那正是并发保护该有的样子**。但原来一律记成「心跳出错」，于是控制台里每半分钟出现一次「已经有「抓取」在进行中（6 秒前开始）」，看起来像同时跑了好几个实例，而事实恰恰相反。给拒绝带上 `reason: 'busy'`，上层据此记成「跳过这次」。<br><br>这依赖同一天加的另一件事：**错误码要过得了 offscreen 那道界**。原来只送 `error` 字符串，`SessionError('session_expired')` 到了另一边就只剩一句话——于是「重试失败：当前未登录豆瓣」这种把会话过期说成功能故障的报错才会出现。 |
| F-04f | 手动补抓当前页 | P1 | 「把我正在看的这页存进档案」按钮，兜底爬虫走不到的角落。 |
| F-04k | **框架标志按结构认，不按显示文字** | P0（2026-07-30 实测修正） | 广播路线原来靠 `<title>…广播</title>` 认页面。那在 2022 年的档案上是对的，但豆瓣把标题改成了**「我的动态」**——于是真跑第一页就判不出来，一条都没抓到，而失败信息只有一句「缺少 1 个页面框架标志」。<br><br>教训不是「把新标题也加上」，而是**别拿显示文字当结构标志**：标题会改名、会本地化、会做 A/B。拿旧档案 403 个广播页（2022-12 → 2024-08）挑出替代标志：`class="stream-items"` 与 `id="db-usr-profile"` 各命中 **401/403**（缺的 2 个是登录页，本该不匹配），与标题一样稳，**但活过了那次改名**。真实的越界终止页两个标志都还在，所以照旧判 `ok`。<br><br>另加一条**完全不依赖 markup** 的 `urlAnchor`：最终 URL 必须还是这条路线。改版能改标题、能改 class，改不了「你请求的资源是什么」。它挡的是被跳到首页——首页信息流同样有 `stream-items`，单看 markup 会认错。<br><br>失败信息现在说出**缺的是哪一个标志**、指出最可能是改版、并说明这一页已如实存进档案可据此重新校准，不必重抓。夹具与真实页面（`test/fixtures/broadcast-2026-07.html`）对齐。 |
| F-04j | 数字 uid 取自全局导航 | P0（v0.5 补，2026-07-30 按真实页面校准） | uid 是档案的归属主键，取不到就开不了工。原先只找广播条目上的 `data-uid`——而**个人主页上不一定有任何广播条目**，于是「开始抓取」报「取不到数字用户 ID」。<br><br>第一版修法是「多找几个模式」（头像 URL、数字形式的 `/people/<id>/`、任意 `uid=`）。**那是错的**：这些位置在个人主页上是本人，在**作品详情页**上却是评论者/别人。而 `SessionGuard.verify()` 会拿这个 ID 在每一页比对，不一致就判 `account_switched` 并**整场停机**——于是抓取会在第一张作品详情页上假装账号被换掉然后停下。**取错比取不到糟糕得多。**<br><br>正确的取证点只有四个，全在**全局导航**里（每张登录后页面都有，包括作品详情页，且语义上就是「当前登录的这个人」）：`_GLOBAL_NAV.USER_ID`、`UPLOAD_AUTH_TOKEN` 前缀、piwik `setUserId`、导航项 `data-moreurl-dict` 里的 `uid`（**必须按 HTML 实体转义后的样子匹配**）。因此也不再需要「退到广播列表页」那条退路。校准依据：`test/fixtures/profile-2026-07.html`（真实页面片段，已删去第三方数据）。<br><br>失败原因是独立的 `missing_user_id` 而不是 `session_expired`——混成「请重新登录」会让用户反复登录去修一个改版问题。 |
| F-04h | 跟随并捕获短链跳转 | P1 | **v0.5 实测新增**：真实档案里的广播含有 `douc.cc` 短链（豆瓣自家的缩短服务），真实目标 URL 藏在跳转后面。**只存短链等于在档案里留一个指向第三方跳转服务的死指针**——那个服务停掉，这条链接就永远解不开了。必须跟随跳转，并把 **3xx 响应本身也写进 WARC**（WARC 本来就为此设计），让整条跳转链在档案内自洽。 |
| F-04g | 已抓判定 | P2 | 不可变路线抓过即跳过；可变路线按 digest 决定写完整记录还是 revisit 记录。 |

#### 关于「原始字节」的一个硬限制（v0.3 新增，需要正视）

CLAUDE.md 要求「Raw bytes, raw headers, no cleanup」。**用 `fetch()` 做不到**：

- **响应体**：`fetch()` 交给你的是**已经解过 `Content-Encoding` 的**内容。gzip/br 压缩的原始字节流拿不到。
- **响应头**：`Headers` 对象是被过滤过的。`Set-Cookie` 等被屏蔽，头的原始顺序和大小写也丢了。
- **状态行**：HTTP 版本信息拿不到。

可选的绕法各有代价：

| 方案 | 能拿到 | 代价 |
|---|---|---|
| `fetch()` | 解码后的体 + 过滤后的头 | 不是原始字节 |
| `chrome.webRequest`（MV3 仍保留只读版） | **真实的响应头** | 拿不到响应体；需与 fetch 结果配对 |
| `chrome.debugger`（CDP） | 真正的原始字节与原始头 | 浏览器顶部常驻「正在调试」横幅，几小时的抓取里这个 UX 不可接受 |

**结论**：v1 走 `fetch()` + `chrome.webRequest` 补响应头，并在 `index.ndjson` 里记一个 **`capture_fidelity`** 字段如实说明这条记录是什么成色（`decoded_body+observed_headers` 之类）。

这不是妥协掉了归档承诺——webrecorder 自家的 ArchiveWeb.page 也是这么做的。**真正不可接受的是做不到却宣称做到了**：十年后有人拿这份档案做取证，`capture_fidelity` 就是他判断能信到什么程度的依据。这个字段属于「丢了就完蛋」的一类，**必须进 spec 第一版**。

### E. 响应分类器（信任层）

| ID | 功能 | 优先级 | 说明 |
|---|---|---|---|
| F-05a | 判定内容而非状态码 | P0 | 豆瓣用 HTTP 200 送封锁页。每个响应产出一个 verdict。 |
| F-05b | 多信号判定 | P0 | 导航栏是否还有用户名、是否跳转 `sec.douban.com`、响应体积相对该路线滚动分布是否异常、预期结构锚点是否存在（附录 A）。 |
| F-03o | **抽取器坏了不许显示成「已验证」** | P0（2026-07-30 实测发现） | 一次真实的舞台剧抓取：3 条全抓到了，coverage 却写着「声称 3 / 抓到 0 / 差值 −3 / **连续性 ✔ 已验证**」。根因是 `interest.list` 的 `idAnchor` 只写了 `/subject/N`，漏掉舞台剧的 `/location/drama/N`、游戏的 `/game/N`、应用的 `/app/N`。<br><br>后果远不止「进度显示 0」：**停滞检测靠「本页有没有新 ID」判断进展**，抽不到 ID 就等于没有终止条件——第 3 页触发停滞，而因为没有缺口，`contiguous` 报 true。对 89 页的电影列表那就是**第 3 页截断 + 声称已验证**。<br><br>三处修：① `idAnchor` 覆盖全部媒介；② `extractItemIds` 按首次出现去重（列表里每条有图片与标题两处链接，3 部剧抽出 6 个 ID，而 `observePage` 把 `ids[i]` 与 `times[i]` 当成同一条目，`highWaterIds` 会记错——那份清单是下次增量在边界上去重用的）；③ `markFinished()` 自检：声称有条目却一个 ID 都没观测到，记 `no_items_observed` 缺口。判据用 `claimed > 0` 而不是「抓过页面」——空列表是完全正常的。 |
| F-03p | 标记列表也有水位线 | P0（2026-07-30 补） | `interest.list` 原来**根本没有 `timeAnchor`**，于是水位线永远 null、`canAdvance` 永远 false——增量抓取对标记列表压根不可能。列表页每条带标记日期（`<span class="date">2025-05-05</span>`）。它**只有日期没有时刻**，那不是页面截断而是豆瓣只公开到天：`parseDoubanTimestamp` 补零点并把 `precision` 标成 `'day'`，`raw` 保留原样。日精度的下界配合闭区间比较意味着「那一天整天重走」——安全（重复免费，空洞永久），但下游必须知道那个 `00:00:00` 是填的。 |
| F-05c | 未分类即失败 | P0 | 判不出来一律当失败停下。「大概没事」是这套系统里最危险的一句话。**2026-07-30 补上了最大的漏洞**：作品详情页此前没有判定描述，于是退回到「HTTP 200 就是 ok」——而那条路线是数千次请求、占档案九成体积、且排在几小时抓取的最后（最可能撞上限流的时候）。一次软封锁会让几千页被标成 ok。现在它有 `urlAnchor` + `anyFrameAnchors`，拿 6341 个真实页面校准过（附录 C.10、C.11）。另加一条与路线无关的规则：**0 字节的 200 一律判 null**——旧档案里就有 7 个被当成数据存下来的空响应。 |
| F-05d | 封锁页也入 WARC | P1 | 将来才能不重抓就重训分类器。 |
| F-05e | 滚动体积分布持久化 | P1 | 分类基线跨会话保留。 |
| F-05f | 金丝雀请求 | P1 | 恢复前确认风控状态。**现成的理想候选**：`.../user/{uid}/interests?ck={ck}&count=1&for_mobile=1`（附录 B.1）——需登录、响应极小、返回形状已知（一个 `total`），既能验会话又能验风控。两个前代实现都没有金丝雀。 |

### F. 人工介入（awaiting_human）

| ID | 功能 | 优先级 | 说明 |
|---|---|---|---|
| F-06a | `awaiting_human` 独立状态 | P0 | 遇验证码/软封锁**必须暂停，不能重试**——在软封锁上重试正是把限流升级成封号的标准路径。 |
| F-06b | 通知用户 | P0 | 系统通知 + 图标角标，说清现在要人做什么。 |
| F-06c | 在普通标签页解决 | P0 | 引导用户在正常标签页完成验证。插件与用户共享会话——这是「在用户浏览器里抓」的架构回报。 |
| F-06d | 恢复前复验 + 降速 | P0 | 先发金丝雀确认恢复，再以更慢节奏继续。 |
| F-06e | 暂停即写 checkpoint | P0 | §3.5。任何暂停都产生一个可恢复的存档点。 |

### G. bundle 写入器

| ID | 功能 | 优先级 | 说明 |
|---|---|---|---|
| F-07a | WARC 记录构造 | P0 | 标准 WARC，不加私货扩展。每条记录独立 gzip member。 |
| F-07i | **不写 request 记录** | P0 | WARC 允许把请求也记下来，乍看更完整。但请求头里带着**用户的 Cookie**，而 bundle 是要导出、甚至可能公开的——把会话凭据写进一份准备分享出去的档案不可接受。请求侧真正有归档价值的信息（URL、时间、intent）已经在 `index.ndjson` 里，没有损失。 |
| F-07b | 即时追加写 | P0 | §2.3 的四步顺序，不攒批。 |
| F-07c | `index.ndjson` | P0 | 每次抓取一行：capture_id、warc_record_id、URL、intent、verdict、observed_at、路线、所在段与偏移、digest。 |
| F-07d | `manifest.json` | P0 | spec_version、bundle_id、previous_bundle_id、插件版本、账号、时间范围、段清单、计数、coverage 对账、**抓取存档信息**。 |
| F-07e | OPFS 暂存 + 崩溃恢复 | P0 | §2.3 的截断修复流程。 |
| F-07f | 段轮转 | P0 | 默认 256 MB。 |
| F-07g | 三种时间戳分离 | P0 | `occurred_at` / `recorded_at` / `observed_at`，**永远不许合并**。同时保留响应 `Date` 头，用于日后校正客户端时钟偏差。 |
| F-07h | 逐字段 digest | P1 | NFC 规范化、去尾空白、统一换行，仅此三项。**不折叠简繁、不折叠大小写**——那些是真实编辑。逐字段算，改评分才不会看起来像改了评论。 |

### H. 存储管理（本次新增）

MV3 的实际限制：`chrome.storage.local` 默认 **10 MB**（Chrome 113 及更早为 5 MB）；申请 **`unlimitedStorage`** 权限后，`chrome.storage.local`、IndexedDB、Cache Storage、**OPFS** 一并解除配额。`chrome.storage.sync` 永远受限（约 100 KB），**绝不能用来放档案数据**。

| ID | 功能 | 优先级 | 说明 |
|---|---|---|---|
| F-08a | 声明 `unlimitedStorage` | P0 | 不声明的话几百 MB 的档案根本放不下。 |
| F-08b | 申请持久化存储 | P0（已澄清） | `navigator.storage.persist()`。不申请的话浏览器在磁盘压力下**可能直接清掉**几小时的抓取成果。UI 要显示是否已获批。 |
| F-08c | 开抓前空间预检 | P0 | ✅ `navigator.storage.estimate()` 比对**含目录页**的预估体量（800 MB × 1.5 余量）。查不了时返回 `null` 并如实显示「查不了」——不当成「够用」。 |
| F-08d | 存储管理页 | P0 | 🟡 已在调试标签里落地（列表、逐份删除、清空全部、配额），正式化时移到独立页面。列表走面板的只读 Worker；**删除经由 offscreen 那条唯一的写入路径**，安全检查放在那边——「现在在抓哪一份」这个知识只在那里。判断逻辑全在 `src/storage/storage-usage.js`（纯函数，Node 里测）：正在抓的绝不许删；确认框要说出哪一份/多大/导出过没有；导出状态分「已导出／未导出／不确定」三档，`unknown` 按最坏情况警告。 |
| F-08e | 导出 bundle | P0 | ✅ File System Access API 选目录，**分块流式**写出（`createWritable()`，不经 JS 堆）。目录非空默认拒绝——用户在选择器里随手点中的可能是文档目录，而覆盖没有回收站。 |
| F-08f | 导入 bundle | P1 | 读回旧 bundle 以恢复**抓取存档信息**与 checkpoint，用于增量抓取或换机器。 |
| F-08g | 删除 bundle | P1 | ✅ 强确认，确认框逐条列出要失去的东西。界面确认框与代码守卫（`checkDeletable`）**两者都有**——用户可能点得很快，而消息也可能是从别处发来的。「已导出」只在导出**校验通过**时才记（在 IDB 里，派生状态）：没验过就说已导出，等于给了一个我们没资格给的保证。 |
| F-08h | 导出完整性校验 | **P0**（升级） | ✅ 回读**目的地**逐个核对字节数与 manifest 里的 sha256。升级理由：导出之后用户会删掉 OPFS 那一份，所以一次悄悄截断的导出不是「少了几个字节」，而是唯一的副本没了。校验必须验目的地——配额耗尽、U 盘拔了、写入中断，这些只在那一侧看得见。结果分「已校验／只验了字节数／没验」三档，不折成布尔。 |
| F-08i | 中断续导 | P1 | 导出被打断能接着来。 |

### I. 增量抓取

| ID | 功能 | 优先级 | 说明 |
|---|---|---|---|
| F-09a | 读上次水位线 | P1 | 从 IndexedDB 或导入的 bundle 取 `high_water_time` 作为本次下界。 |
| F-09b | 不可变路线跳过 | P1 | 广播文本发布后不可编辑，抓过不再回访。 |
| F-09c | 干净完成才推进水位线 | P1 | §3.3 的不变量，在代码里显式断言。 |
| F-09d | 删除推断**只在全量重枚举的路线上成立** | P1 | v0.3 细化。**水位线路线（广播）**：只走到下界就停，下界以下没看，缺失无法与「没抓到」区分 → 绝不推断删除。**全量重枚举路线（标记类列表）**：本次把整份列表从头到尾走完了，那么「上次有、这次没有」是**有意义的信号**。tofu 正是用 `version` 戳做到这一点（附录 B.5）。插件只如实记录本次枚举到了什么 + 是否全量，判断仍留给 parser。 |

### J. MV3 生命周期

| ID | 功能 | 优先级 | 说明 |
|---|---|---|---|
| F-10a | 心跳保活 | P0 | ✅ `chrome.alarms` + offscreen document。service worker 约 30 秒空闲即被杀，对几小时的抓取是致命的。 |
| F-10b | 无状态恢复 | P0 | ✅ 进度状态全在 IndexedDB（`IdbKvStore`），内存里不留唯一副本。**曾经用 `chrome.storage.local` 走过弯路**：offscreen document 拿不到它，而让 offscreen 借道 service worker 会形成请求/响应环（SW 正 await offscreen 的响应，offscreen 又 await SW），表现是「刚 set 完就 get 不到」。IndexedDB 是标准 DOM/Worker API，三种上下文都能直接用、看同一份数据——这条设计一开始就是对的，偏离它才是错。连带结果：`storage` 权限不再需要。 |
| F-10b2 | **「继续」必须真的继续** | **P0**（v0.5 补） | 报上来的现象是：点暂停再点继续，弹出的是一条「需要你处理：你手动暂停了抓取」，而且每 30 秒再弹一次。**同一个 bug 在三层各有一份**——① `Frontier` 还停着，于是下一批立刻又返回同一个停机原因（补 `clearStop()`，顺带把 `awaiting_human` 的条目放回队列——那个状态是**连带阻塞**整条路线的，不放回去这条线永久堵死）；② `runner.resume()` 见到 `active` 就当「已经在跑了」直接返回，而 `active` 的含义是「这次抓取还在内存里」，不是「正在发请求」（同一个混淆在界面上也咬过一次，docs/ui.md §4.4）；③ service worker 那份调度镜像还写着 `user_paused`，而**心跳唯一的判据就是它**——于是每次心跳都判定「不恢复」并再弹一次，worker 一被杀更是再也不回来（补 `supervisor.resumeRun()`，把原因改回崩溃哨兵）。 |
| F-10b3 | **心跳必须每次都推进一段** | **P0**（v0.5 补） | `Supervisor._running` 原来只有 `pauseRun()` / `finishRun()` 会清，而一段推进的**正常**结局是「预算用完了，还没抓完」——两个都不会被调用。于是它永远是 true，此后每次心跳都直接返回「已经在跑了」，**抓取就此停住**，只有 worker 被杀、内存清零之后才会再走一段。日志里的样子：一次「推进结果 …captured:25…」，然后连续十几次「心跳 → 未恢复」。改成在 `finally` 里清（一段推进抛异常也要清，抓取里出错是常态）。要防的是**并发**不是先后：测试相应改成「并发三次只开工一次 + 先后三次各推进一段」——原来那条测试把 bug 当成了规格。 |
| F-10b4 | **驱动层空转检测** | **P0**（v0.5 补） | 症状是导出的日志整整 500 行 `batch`、一条 `capture` 都没有，时间跨度 7.7 秒——每秒六十几批，**一个请求都没发**。真凶是 `resume()` 漏写了 `maxCaptures` / `capturedSoFar`：`Math.max(0, undefined - undefined)` 是 NaN，`Math.min(25, NaN)` 是 NaN，`while (0 < NaN)` 直接为假于是一批里什么都不做；而 NaN 又通不过 `hitCap` 的比较，`done` 保持为假。这个 bug 一直潜伏着，是「继续」修好之后才第一次走到（F-10b2）。**预算挡不住它**——22 秒到了就退出，心跳 30 秒后再叫醒，空转重来一次，能烧到用户合上电脑为止；而豆瓣那边什么都看不到，不会有外力把它撞停。所以修了三层：① `resume()` 与 `start()` 写同一组字段；② `runBatch` 用 `Number.isFinite` 判上限，不再用 `=== null`；③ `driveWithinBudget` 加空转检测——连续 3 批 `captured === 0 && failed === 0 && !done` 就以 `driver_stalled` 停下并报出队列状态。第三层是**独立于成因**的那一层：设计里对翻页早就写着「靠停滞检测终止，而不是靠『没有新条目』」，这里是同一条道理在驱动层的应用。顺带把 `maxCaptures` 写进指针，否则一次「最多抓 10 条」的试跑在心跳恢复后会变成全量抓取。 |
| F-04j | **闸门跟着连接走，不跟着活动走** | **P0**（v0.5 补） | 并发恒为 1 是靠四层叠出来的：`Exclusive` 保证同一时刻只有一件活动（F-04i）；`driveWithinBudget` 与 `loop.run()` 全是顺序 `await`，一次一条；`Frontier` 里 `in_flight` **连带阻塞整条路线**，所以同一条线上不可能有两页在飞；`RequestGate` 用一条 promise 尾链串行化，**即使被并发调用也会排队**。作品详情页不是第二条执行路径——它由列表页派生后进同一个 frontier，由同一个循环挨个取。**但闸门原来是按活动建的**，而闸门的第一个请求是不等待的（`_lastFinishedAt` 是 null），于是「身份确认 → 开工探测」这两发之间隔 0 毫秒；每次崩溃恢复同理。不是并发，但同样违反「1 秒一个」——豆瓣看到的只有请求，它不关心我们内部把它们算作几段活动。改成整个 runner 共用一个闸门，Pacer 可以换（它带着退避层级，要跟 checkpoint 走）而计时与排队状态留在闸门里。 |
| F-04k | **抓取顺序真的按优先级走** | **P0**（v0.5 补） | 报上来的现象：广播抓了 40 条还在「进行中」，同时电影、音乐、书、游戏、舞台剧十几条线一起在爬。成因是 `Frontier.enqueue` 的 `priority` 默认值是 50，而种子带着优先级入队、**翻页却没带**——广播第 2 页（50）于是输给了每一条标记列表的种子（40）。后果不是「顺序稍微乱一点」：**第一页之后整个优先级设计就失效了**，所有路线并列在 50 按入队顺序轮转，而排序的全部意义在于「中途被打断时，先跑完的一定是最难补的」。第二层是同优先级内也要**深度优先**：15 条标记列表优先级完全相同，翻页追加到队尾就成了轮转，中途一停得到的是十五份半截列表——每一份都不完整、连续性都证明不了、都不能推进水位线。改成同优先级内优先继续已经开工的那条（同一条路线内部仍是先进先出，分页必须按页序走）。 |
| F-10b5 | **恢复之后进度接着数** | **P0**（v0.5 补） | `RouteState` 活在内存里，而一场几小时的抓取会跨越很多次 service worker 死亡。恢复时新建 `CrawlLoop` → 所有计数归零 → 界面上的「已抓」显示的是「上次恢复以来抓了多少」，用户读到的是「一共」；更糟的是 `stateFor()` 是懒的，恢复之后整张进度表先空掉，等抓到东西才一行行长回来。起点取自 `index.ndjson`（`recoverBundle` 本来就在读它，按 `route_key` 汇总 `item_count`，只数 `verdict: ok` 的），并在建 loop 时立刻把这些路线的状态建出来。**连带的坑**：`markFinished()` 里那道「声称有条目却一个 ID 都没抽到 = 抽取器坏了」的自检原本判 `capturedCount === 0`，接上历史计数之后它会永远沉默——改成判**本次会话抽到过多少 ID（含重复）**，那才是「抽取器在不在工作」的正确判据（只数 `newIds` 也不行：恢复后只读到重复页时 `newIds` 合法地是 0，会误报）。另外**叶子路线原本一次都不计数**：成功的作品详情页捕获不碰任何 `RouteState`，于是那一行抓了几千页仍写着「已抓 0」，而且**只在出错时才出现在表上**（失败会记缺口，从而建出状态）——一条只在坏掉时现身、且永远写着 0 的进度行比没有更糟。 |
| F-04l | 作品详情页去重：**同一次会话内去，跨恢复不去** | 已定 | 一部电影可以同时在「想看」和「看过」两张列表上（刚看完还没删想看），两张都会派生出同一个详情页。同一次会话里 frontier 按 `url_key` 全局去重（跟踪参数会被剥掉，所以两张列表上的链接归一到同一个 key），只抓一次。跨恢复则去不住：checkpoint **只保留未完成的条目**，已完成的在 index 里——那是刻意的，重复记录会带来两个可能不一致的真相来源。代价是崩溃时正在飞的那张列表页恢复后会重抓，它派生的详情页也跟着重抓一遍（一次崩溃约十几个请求）。**不去消除它**：规范要求 `capture_id` 唯一而**不要求 URL 唯一**，同一个 URL 的多次捕获正是 WARC 存在的理由；而两次之间豆瓣页面可能真的变了（评分、短评），两份都留着更符合「捕获更多而非更少」。`test/subject-dedup.test.js` 钉住的是「重抓不会把档案写坏」：两条 index 记录、两个 capture_id、两份不同的 `content_sha256`、都能取回、「验一验」通过、参考校验器通过。 |
| F-10b6 | **被恢复过的抓取必须收得了尾** | **P0**（v0.5 补） | 报上来的原样：`data-…-00001.warc.gz: record_count 为 219，但 index 中指向本段的行数为 0。段与索引已失去对应关系。`——而那份 index 文件里**确实有 219 行**指向那一段（拿真实档案验过：219 行、`by_verdict: {ok: 219}`、`recoverBundle` 报「无需修复」）。坏的不是档案：`IndexWriter` 压根没有恢复路径，`_lineCount` / `_counts` / `_perSegment` 每次构造都归零，而 `SegmentWriter` 那边**是**从磁盘恢复 `record_count` 的，于是收尾的交叉核对必然失败。影响面是「全部」——一场几小时的抓取必然跨越很多次 service worker 死亡，也就是说**正常的完整抓取一次都收不了尾**。修两层：① `recoverBundle` 交还 `indexStats`，`BundleWriter` 传给 `IndexWriter`；② **收尾时按 index 文件重算**行数与每段计数——文件才是权威（追加写、每页落盘），内存计数器活在一个随时被杀的 worker 里，两者分叉时错的一定是内存那边。第二层让这个交叉核对变得更硬：它现在比的是「段自己声称写了多少」与「index 文件里真的有多少行指向它」，两个独立来源。③ 顺带：收尾抛错原本一路冒到 `onResume` 无人接管，心跳每 30 秒重试一次、刷「心跳出错」，而界面上什么都不显示——现在停在 `finalize_failed`（**绝不自动重试**：收尾是确定性的，同样的档案同样的代码只会同样地失败）并明确告诉用户「抓到的每一页都已落盘，坏的只是最后写 manifest 那一步」。 |
| F-10b7 | **连续性证明必须活过崩溃** | **P0**（v0.5 补） | 一份 `status: complete` 的真实档案，21 条路线**全部**写着「有 1 处缺口，原因：aborted」、水位线全空、`claimed` 全空——而那次抓取一次都没被风控打断过，只是中途崩过、恢复过。checkpoint 的 `routes[]` 原来只存 `{cursor, stall}`：够续上翻页，不够重建连续性证明。恢复之后每条路线都是崭新的（没有水位线、没走完、没被打断），收尾时 `flushRouteEvidence()` 把它们全部记成 `aborted`。**后果不只是难看**：`advanced` 永远是 false，增量抓取永远不可能；而 `claimed` 来自抓取那一刻的页面，事后抓不回来，丢了就是永久丢失。还有一层更隐蔽的：checkpoint 里的 frontier 条目**没存 `cursor`**，于是恢复之后下一页按 `route.pagination.first` 从第 1 页重算——算出来的页码要么早抓过（被去重挡掉，这条线**静默停住**、永远等不到停滞终止），要么把走过的页码再走一遍。修：`RouteState.serialize()/restore()`（含 `seen_ids`、水位线、gaps、finished/stopped、claimed）、frontier 条目带上 cursor、规范 §7.1 写明这条要求。判据就是规范里那句：**恢复之后产出的 manifest，必须与「一次不间断跑完」产出的那份逐字段一致**——测试直接比对两份。 |
| F-10b8 | **恢复之后不许倒着翻页** | **P0**（v0.5 补） | 真实日志：恢复后抓 `p=20`（对的），然后 `p=2`、`p=3`……一路重抓到 `p=19`，接着**换去抓标记列表**——而广播远没抓完。两步：① checkpoint 的条目没记 `cursor`，「下一页」于是按 `route.pagination.first` 从第一页重新数；② 重抓的那些页全是重复条目，**停滞检测把它当成「这条线走完了」**，于是 `markFinished()`、推进水位线、放开门控。②才是严重的那一半：一次**假的完整性声明**，而且连锁后果是拿最不可替代的东西（广播可静默删除）去换最可替代的（作品详情页随时能重抓）。三层防护：`cursor` 进 checkpoint；缺了就**从 URL 反推**（救得回升级前写下的半成品档案）；`Frontier.markCaptured()` 用 index 里 `verdict: ok` 的 `url_key` 给去重集合打底——checkpoint 只留未完成条目，抓完的那些恢复后一律不认识，任何把旧页码算回来的 bug 都会变成真实的重抓。顺带统一了三处 `url_key` 的归一化（种子、翻页、恢复），此前恢复那处直接拿原始 URL 当键，去重跨不过那条边界。 |
| F-10b9 | **`startRun()` 不许占住「正在推进」那个标志** | **P0**（v0.5 补） | 新开一场抓取，跑完第一段（captured 25）之后每一次心跳都是「未恢复」，直到重新加载扩展。`startRun()` 把 `_running` 置成 true，而真正推进的是紧随其后由 background 直接调用的 `drive()`——它不走 `tick()`，也就没人来清。这是 F-10b3 那个标志的**另一个写入点**：上次修的是 `tick()` 里忘了在 finally 清，这次是压根不该写。并发防护本来就有三层且都不依赖它（`Exclusive` 全局互斥、offscreen 的 `driving` 复用同一个 promise、`tick()` 自己的检查）。 |
| F-05h | **个人主页可自定义，判定只认与版式无关的标志** | **P0**（v0.5 补） | 个人主页与 5 个分类入口页原本**没有判定描述**，走的是兜底分支「`status === 200` 就算 ok」——而豆瓣的封锁页返回的正是 200，个人主页又是一次抓取里的第一张页面：最该被拦住的那一刻反而完全没有拦截，封锁页会被存成 ok、路线标成「连续 ✔」，产出假的完整性声明。补了一份 `profile.page`，但**只认与版式无关的标志**：拿真实档案的 6 张页面量过（含 `location/people` 那个最不一样的舞台剧入口），`db-usr-profile` / `db-global-nav` / `_GLOBAL_NAV` 每张都有，而「我看过的影视」这类区块**只出现在个人主页上**——那正是可自定义的部分。取 `db-usr-profile`（「某人的个人页」这个外壳，装头像与用户名，不是可增可减的模块）。有一条测试直接钉住这条规则：判定描述里出现任何分类区块的标志就红。 |
| F-05i | **发过请求的路线不许从证据里消失** | **P0**（v0.5 补） | `stateFor()` 是懒的，而建状态的几处都要求判定是 ok 或失败。于是一条「唯一那页判成 `gone` / `soft404`」的路线走 `done` 那条路，一个状态都没建——收尾时它从 coverage 与 crawl_state 里**整条消失**，档案里看不出我们试过它。那比写错原因更糟：错的原因还能被质疑，消失的路线没人会想起来去问。改成一发请求就建状态。连带把「一页都没读成过」与「翻页走岔了」分开：前者记 `route_unavailable`（这个分类你没用过、被下线了、或者那页被拦了），后者才是 `next_page_not_queued`——把前者说成后者是**编造一个错误的原因**，而这一行正是给人排查用的。 |
| F-05g2 | 单页路线**抓完那一页就当场标成走完** | P0（v0.5 补） | F-05g 只在收尾时补这一刀，于是整场抓取期间那 6 条（个人主页 + 5 个分类入口）一直显示「进行中」——而它们在第一秒就抓完了。用户看到 6 行永远不动的「进行中」，合理地以为卡住了。判据是**有 `entryUrl` 且没有 `pagination`**：单页路线一页就是全部；翻页路线靠停滞检测或到达下界收尾；而**作品详情页没有 `entryUrl`**（派生集合，条目由列表页陆续入队），必须排除——它的队列中途空掉是正常的，那时标成走完就是假的完整性声明。今天的优先级排序（列表 40、详情页 90）让那一幕暂时撞不上，但那是排序的副作用而不是这条规则的保证，所以判据本身有测试钉死。 |
| F-05g | **单页路线也要能「走完」** | **P0**（v0.5 补） | 真实档案里 6 条路线（个人主页 + 5 个分类入口）全都写着「有 1 处缺口，原因：aborted」——而它们**一次就抓全了**。路线定义里写着 `pagination: {kind:'page', step:1, first:1}`，可 `entryUrl` 压根不收 offset：那个字段是假的。而分页路线只能靠**停滞检测**或**到达下界**才算走完，这两件事在一张不翻页的页面上永远不会发生。改：删掉那个假字段，并把收尾时的定性拆成三种——**不分页 + 队列空 + 无缺口 = 真的走完了**；**分页 + 队列空** = 记 `next_page_not_queued` 并写明「下一页没能入队，最常见是算出来的页码早就抓过、被去重挡掉」（笼统的 `aborted` 看起来像被风控打断，会把排查引向完全错误的方向）；**还有活没干完** = 才是真的 `aborted`。顺带把缺口原因翻译成中文并把 `detail` 显示出来——那句解释原本只存在档案里没人看得到，而界面上还在打内部标识。 |
| F-03r | **静默退化必须报出来** | **P0**（v0.5 补） | 「抽得到条目、抽不到时间」是最隐蔽的一类坏法：翻页照常、连续性照常 ✔ 已验证、界面上什么都不异常——只有「已回溯到」是空的。而后果是 `high_water_time` 永远 null、`advanced` 永远 false，**这条线永远不能增量**。书就是这么坏了很久的（列表页日期后面跟着「读过」两个字，而 `timeAnchor` 要求日期紧接着 `<`），三条书的路线一条时间都抽不到，**没有任何地方报过**。两处补上：抓到条目却一条时间都没抽到 → `RouteState` 记下（**不记成缺口**——数据没缺，缺的是「能不能增量」这个能力，记成缺口会让 `contiguous` 变 false 而那是不实的）；收尾时一条线跑完了却没有水位线 → 报 `no_watermark` 并说清「下次仍然只能全量重走」。只对**判定描述里有 `timeAnchor`** 的路线报——作品详情页、个人主页压根没有时间概念，那是设计如此。 |
| F-03q | **会话复核只比数字 ID** | **P0**（v0.5 补） | 抓到 `book.douban.com/subject/4820710/` 时守卫报「账号发生了变化（用户名 mewcatcher → 163211544）」并整场停机——而用户的账号一秒都没变过。`extractAccountHints` 取用户名的方式是「页面上第一个 `/people/<x>/`」，在作品详情页上那是**某位短评作者**的。`session.js` 里早就写着这个坑（`UID_PATTERNS` 那段说明作品详情页上的 `data-uid` 是评论者的），但 `verify()` 仍然在比 `username` 与 `displayName`——而这两个都是从页面**正文**扒的，正文里到处是别人。改成只比数字 ID：它来自 `_GLOBAL_NAV.USER_ID`，是唯一一个「一定来自全局导航、一定是当前登录者」的字段，也正是档案的归属主键。**多比一个不可靠的字段不是多一层保险，是凭空制造停机。**（`preflight` 与 `discoverUsername` 照旧取用户名——它们跑在个人主页/`/mine/` 上，那里的 people 链接确实是本人。）测试原来把这条 bug 当成了规格（「只有用户名变了也算换账号」）。 |
| F-11i | **把「豆瓣上已经没有了」的条目列出来** | P0（v0.5 补） | 一份 3347 条的真实档案里有 8 条 `gone`：那 8 部电影豆瓣已经删了，网上再也查不到，而档案里存着它们当时的样子——**整份档案里最不可替代的信息**。而它原来只以「判定分布：正常 3339 · 已不存在 8」一个数字出现，没有任何地方说得出是哪 8 条（捕获列表只渲染前 500 行，它们排在第 3000 多条）。档案页单独给它们一块地方，列 URL 与抓取时刻。**日志是同一个问题的另一面**：抓取环只有 200 条而这次抓了 3347 页，于是 8 条 `gone` 里只有最后一条留了下来——判定不是 ok 的捕获改走稀疏环（500 条，专为「稀少且要紧」准备的那个），它们本来就属于那里。 |
| F-11h | **每一种停机都要给得出下一步** | P0（v0.5 补） | `account_switched` 与 `quota` 在界面上一个按钮都不给：用户照提示做完了该做的事（切回账号 / 清出空间），却没有任何地方能告诉豆备「我弄好了」——只能重装扩展。补上按钮，并加一条**行为**测试逐个原因驱动界面，断言至少有一个可点的下一步（例外只有 `failures_pending`：动作在失败清单里逐条重试；以及 `crash`：心跳自己接手）。顺带：停机事件现在都带上**是抓哪一页时触发的**，日志里那一行才答得出排查的第一个问题——这次的答案正是「一张作品详情页」。 |
| F-03p | **停机挡的是 `next()`，不是 `enqueue()`** | **P0**（v0.5 补） | 每按一次暂停，当前那条路线就被截断一次。真实日志：`04:46:16 paused` → `04:46:18 capture interest.game.collect start=120`（在飞的那一页抓完了）→ `04:46:22 resumed` → `04:46:22 capture interest.game.do start=0`——直接换线了。成因：暂停的语义是「当前这一页抓完就停」，所以那次「抓完了 → 入队下一页」**必然**发生在停机标志已经立起来之后，而 `Frontier.enqueue()` 当时会挡掉它并返回 false，`_enqueueNextPage()` 不看返回值。于是 start=135 永远没人记得。**停机该挡的是发出去（`next()`），不是记下来（`enqueue()`）**：入队只是记录「还有这一页没抓」，丢掉它就是凭空制造一个洞；而允许入队没有坏处——停机期间 `next()` 本来就不发东西，这些条目要么恢复后被消费，要么原样写进 checkpoint，而后者正是对的。**测试也曾把 bug 当规格**（「停机后不接受新入队」）。另：复现必须在**一页正在飞**的时候按暂停，批与批之间按是复现不出来的——第一版端到端测试就是那么写的，加不加修复都绿。 |
| F-10b10 | **门控要跟着恢复重开** | **P0**（v0.5 补） | 暂停 → 重新加载扩展 → 继续，结果**一秒就「跑完了」**，几千个作品详情页凭空消失。`Frontier._openGates` 每次新建都是空的，而门控只在抓取过程中被打开（前置路线跑完那一刻）。恢复之后没人重开，于是所有还带着 `gatedBy` 的条目一律取不出来——`hasReady()` 报 false，上层判定「跑完了」，然后收尾写下 `status: complete`。（`interest.item` 那条路线倒是会被记上 `aborted` 缺口，所以不是完全无声，但档案照样提前收了尾。）**不另存一份「哪些门开着」**，而是从恢复出来的路线状态重新推导：门开不开的判据本来就是「前置路线 `canAdvance`」，而那个状态现在跟着 checkpoint 走了（F-10b7）；多存一份就多一处会与真相分叉的地方。 |
| F-10c | 关标签页/重启浏览器可续 | P0 | 重开插件即从 checkpoint 继续。**状态按读者分两处**：完整 checkpoint 在 bundle 目录里（规范要求，可随档案迁移，只有 offscreen 读得了）；调度需要的三个字段（`pause_reason` / `paused_at` / `rate_state`）镜像进 IDB 指针，供 service worker 判断该不该自动恢复——它读不了 OPFS。两者不一致时**以档案为准**，恢复走的是 offscreen 读档案那条路。SW 因此也不把 checkpoint 传给 offscreen：传摘要过去会静默丢掉游标，表现是「恢复之后从头重抓」。 |
| F-10e | **自恢复，不是手动重触发** | P0 | service worker 被杀在几小时的抓取里是**常态**而非异常（一次抓取会被杀几十上百次）。心跳用 `chrome.alarms` 而非 `setTimeout`——后者活在 worker 内存里，worker 一死就没了；闹钟由浏览器持有，跨 worker 生命周期、跨浏览器重启存活，系统休眠期间挂起、醒来补发。**它是唯一一个我们死了它还在的东西。** |
| F-10f | **自动恢复只针对意外中断** | P0 | 醒来后**不能无条件接着抓**。停下来的原因分两类：意外中断（被杀、崩溃、休眠）可以自动继续；刻意停下（风控、验证码、会话失效、用户暂停、配额）一律等人——停下来本身就是保护措施，自动恢复等于绕过它。醒来就重试软封锁正是把限流升级成封号的路径。未知的停止原因保守处理，不恢复。 |
| F-10g | 崩溃哨兵 | P0 | worker 被杀时没有机会写任何东西，指望临终留言不现实。所以**反过来**：开工前先写一个 `pause_reason: crash` 的 checkpoint，正常暂停或结束时再改写——「没来得及改写」本身就是崩溃的证据。方向是刻意选的：宁可把正常结束误标成崩溃（代价是多做一次幂等的恢复检查），也不要把崩溃误标成正常（代价是数据对不上却无人察觉）。 |
| F-10h | 崩溃不得洗掉降速 | P0 | 退避层级跨会话保留在 checkpoint 里。崩溃恢复前必须先等够冷却，否则「崩一次就恢复原速」会变成绕过退避的后门。 |
| F-10d | OPFS 写入走 Worker | P0 | ✅ 已落地，形状比原计划更彻底：**整条抓取链**跑在 offscreen document 里（fetch、判定、WARC 组装、gzip、落盘），由它起一个专用 Worker 写 OPFS。原因是字节过不去——`chrome.runtime.sendMessage` 只认 JSON，`Uint8Array` 过去会变成 `{"0":1,…}`；只搬「落盘」这一步的话，每条 WARC 记录都要在 SW ↔ offscreen 之间过一次 JSON。service worker 退回它唯一擅长的事：拿着闹钟。 |

### K. 界面

| ID | 功能 | 优先级 | 说明 |
|---|---|---|---|
| F-11a | ~~popup 概览~~ | **已并入 F-11b** | 曾经是「点图标弹一个小窗：状态、进度、暂停/继续」。做出来之后它一直是个多余的中间层——日志、覆盖率、档案预览、失败页面一个都放不下，而且**一失焦就关**，恰好与「盯着一个跑几小时的任务」相反；实际用法从来都是「点图标 → 再点一下进面板」。拆掉之后点图标直接开面板（已开着就切过去）。附带收益：状态不必在两处渲染、两处撒谎、两处测，而 F-04i 挡的头号并发场景「popup 和面板都点了开始」本来就是它自己造出来的。**坑**：manifest 里只要还留着 `default_popup`，`chrome.action.onClicked` 就永远不触发——点图标什么都不会发生；`test/manifest.test.js` 钉住了这一点。 |
| F-11g | **「已回溯到」用每页中位数，不用全局最小值** | P0（v0.5 补） | 真实数据：一份 20 页、严格新→旧的广播列表，唯独第 10 页混着一条 2018 年的（第 9 页 2025-12-09，第 10 页 2018-08-18，第 11 页 2025-08-29）。`lowWater` 是全局最小值，从第 10 页起界面上那一列**再也不动**——而抓取还有一大半没跑完，用户合理地怀疑它卡住了。那个值没说谎，只是它回答的不是「抓到哪儿了」。拆成两个量：`low_water_time`（全局最小值，进 manifest，抓完之后「这份档案往回覆盖到 X」正是它）与 `progressTime`（每页**中位数**的累计最小值，界面用）。中位数是因为一条离群值动不了它；累计取最小是因为进度只能往前不能回头——来回跳比不动更让人不安。 |
| F-11k | **中止一次抓取** | P0（v0.5 补） | 暂停是「等会儿接着抓」——档案还在写、指针还指着它，所以**删不掉**；而存储页那句「这份正在抓，先暂停或等它结束」是句错话，用户照做会发现什么都没变。补一个**中止**：收尾成 `aborted`（如实带上缺口、绝不推进水位线），放开「当前这次抓取」的指针，之后它就是一份普通的已收尾档案——能看、能导出、能删。`checkpoint.json` **留在档案里**（规范 §3.1 要求 `aborted` 必须带，那样半成品搬到别的机器还能接着抓），清掉的只是我们自己 IndexedDB 里的指针，两者是不同的东西。确认框要说清**不可逆的是这次抓取，不是数据**——说反了用户要么不敢按，要么按了才发现丢进度；并且要提一句「下次是增量」，否则「重新开始一次」听起来像要再等几小时。 |
| F-11j | **抓取方式由用户选** | P0（v0.5 补） | 三个：**增量**（默认）、**全量重抓**（上一次有缺口、或者不信任它）、**增量 + 重抓作品详情页**（想看评分/短评变了没有）。默认帮用户省时间，但**不替他做决定**——尤其当默认行为会**跳过**东西的时候。第三个有个坑：作品详情页由列表页上的链接派生，而增量模式下列表页只抓到下界为止，所以「不跳过已有的」远远不够——能重抓的只有最新那几页上的十几个。必须把链上已知的详情页 URL **直接排进队**（门控照旧，仍然要等广播抓完）。说到做不到比没有这个选项更糟。 |
| F-11f | **「正在做什么」由做事的那一端报出来** | P0（v0.5 补） | 开工要先确认账号（抓个人主页取数字 uid + 确认登录状态，两次真实请求、过节奏闸门，好几秒）。这段时间**既没有 runner 也没有 checkpoint**，界面照着状态渲染只能说「没有进行中的抓取」——而用户刚点了开始，看到的是「正在确认账号…」→「没有进行中的抓取」→（很久之后）「正在抓取」。第一版让界面本地 `setState` 一个乐观状态，那是错的：两秒一次的轮询读到真实状态就把它盖掉，而且活不过面板刷新。正确做法是把全局互斥锁（F-04i）的持有者一路带到界面（`offscreen → background → panel`）——它本来就知道现在在干什么。界面只保留**开口那一小段**的兜底（从点下去到 offscreen 拿到锁之间，可能还要先建 offscreen），一旦后端报得出来就以后端为准。**透传那一环真的漏过**：offscreen 报了，background 的 status 分支没带上；现在有一条测试专门钉这一层。 |
| F-11e | 路线中文名由路线定义生成 | P1（v0.5 补） | 界面上曾经混着出现 `interest.drama.wish`、`interest.game.do` 这样的内部标识——路线是**生成**的（媒介 × 状态的笛卡儿积），名字却是手抄的一张表，漏了五条，而且漏的都是不常用的那几个状态。改成按同一组维度生成（状态词跟着媒介走：书是「读过」不是「看过」），并用一条测试把两边钉死：`buildRoutes()` 吐出来的每个 key 都必须有中文名，加媒介或加状态时测试先红。认不出来的**原样返回 key** 而不是「未知路线」——丑是能被看见的 bug，后者彻底断了线索。 |
| F-11b | 完整面板（独立页） | P0 | **唯一的界面。** 范围选择、逐路线进度、覆盖率对账表、日志、档案预览、导出。 |
| F-11e | **进度不用豆瓣的计数当分母** | P0 | 计数不可信（§3.1），拿它当百分比分母会给出一个看起来很可信的假数字——前代正是这么干的。改用诚实的表达：时间边界路线显示「已回溯到 2023-04-12」，全量枚举路线显示绝对条数。详见 docs/ui.md §4。 |
| F-11f | **覆盖率页说事实不下判断** | P1 | 差值不用红色、不写「缺失」——它不是错误。完整性只看连续性那一列。`null` 与 `0` 在界面上也必须分开显示。 |
| F-11g | **每个坏消息都带一个动作** | P0 | `pause_reason` 直接决定文案与可用按钮。要明确告诉用户「插件不会自动重试」——那是在保护他的账号，不是卡住了。 |
| F-11c | 中文界面 | P0 | 面向用户的文案一律中文。 |
| F-11d | 本地日志与诊断导出 | P1 | 全部本地，默认零遥测。导出诊断包**须先脱敏**。日志分**两个环、各自限额**：稀疏事件（重试、停机、错误、门控）500 条，抓过的 URL 200 条。`batch` 与 `page` 一个字都不记：前者是内部记账（跑完一批），后者与 `capture` 一一对应——**日志记的是发生了什么事，不是循环转了几圈**。这条是踩出来的：一次驱动层空转（F-10b4）让 `batch` 以每秒几十次刷屏，导出的 500 行里唯一有价值的是最后那行 `paused`。合在一个环里的话，一次全量抓取的几千页翻页记录会把「为什么停的」「哪一页反复失败」全部挤出去——而那几条正是事后唯一能查的东西。读的时候两个环按时间合并成一条时间线，因为「停之前最后抓的是哪一页」要的正是前后文。完整且权威的抓取记录始终是档案里的 `index.ndjson`（带偏移量与摘要），日志只是给活人看的近期窗口。 |

### L. 发布前的隐私闸门

| ID | 功能 | 优先级 | 说明 |
|---|---|---|---|
| F-12a | 第三方内容标注 | P1 | 抓取时就标出「作者不是你」。**v0.4 实测**：广播条目直接带 `data-uid` 属性，与本人 uid 不同即为他人内容——比解析 URL 干净得多。实测每批有 21～27 条属于他人。site-generator 默认排除要靠这个标注。 |

### M. 通往下游的入口

| ID | 功能 | 优先级 | 说明 |
|---|---|---|---|
| F-13a | 下游工具入口 | P1 | UI 里给出 parser / site-generator / NeoDB adapter 的说明与链接，兑现 README 承诺。 |
| F-13b | 插件内跑 parser | P2 | 值得做，但绝不能让 parser 只能在插件里跑。 |
| F-13c | 文件导入器 | P2 | Letterboxd / Goodreads 等有原生导出，属导入器不属爬虫，位置更可能在 parser 侧。 |

---

## 5. 两个状态机

**frontier 条目**

```
pending ──▶ in_flight ──┬─▶ done
                        ├─▶ failed ─────▶ pending（有限重试，仅限网络层错误）
                        ├─▶ awaiting_human ──▶ pending（金丝雀通过后，降速）
                        └─▶ terminal_stop（会话失效 / 账号切换 → 整场停机）
```
`failed` 不允许跳过（F-03h）。`awaiting_human` 不允许自动重试（F-06a）。

**响应 verdict**

```
ok        → 入 WARC，继续
blocked   → 入 WARC，转 awaiting_human
challenge → 入 WARC，转 awaiting_human
login     → 入 WARC，整场停机（写 checkpoint）
gone      → 入 WARC，标记条目消失，继续
soft404   → 入 WARC，视同 gone 但单独统计
(其他)    → 失败停下（F-05c）
```

---

## 6. 待决问题

1. ~~**要不要抓条目详情页？**~~ **v0.5 已定：抓，且是必需项，但必须排在广播全部抓完之后。**（用户决定。）

   **理由（用户给出，比我原先的理解更根本）**：作品详情页是**生成静态豆瓣站这个最终目标的必要输入**。列表页只有标题、年份、评分和缩略图，撑不起一个像样的作品页。

   这条理由把它的性质变了。原本我按「目录数据 = 可替代 = 可选」归类，但架构里有一条硬约束：**「丢掉所有派生存储、从捕获重建，必须能离线完成、零网络请求」**，而且「标题、年份、评分、海报 URL 都已经在 WARC 里，外部数据源只用于交叉链接和别名」。既然站点渲染要靠详情页的元数据，那么**不抓详情页，离线重建这条不变量就不成立**。

   所以它不是可选增强，是**离线重建的必要输入**——只不过仍然可以在源站还在时重抓，因此留存等级依旧是 `catalog-*`。

   体积代价（**692 MB / 90.3%**，附录 C）依然成立，所以用结构手段把代价关住，而不是靠不抓来回避：
   - **单独成段 `catalog-*`**（spec §2.1）。**但要说清楚丢掉它的代价**：你保住了自己写的一切，失去的是**离线重新生成完整站点**的能力（作品页会退化成只有标题年份）。所以是「知道代价的可丢弃」，不是「无所谓的缓存」。
   - **M1 必须验证**：作品详情页里确实含有 site-generator 需要的字段（导演、演员、简介、海报等）。如果不含，这条理由就不成立，得重新讨论抓什么。
   - **前置依赖硬编码**：`interest.item` 路线必须等 `broadcast.timeline` 的 `advanced=true` 才允许启动（F-03k）。广播不可编辑、可静默删除、时间最敏感；目录页则永远可以回头再抓。顺序错了就是拿最不可替代的东西去换最可替代的东西。
   - 存储预检（F-08c）必须按**含目录页**的体量估算，否则用户会在抓到九成时撞上配额。
2. ~~**`mode=grid` 还是 `mode=list`？**~~ **实现选了 `mode=grid`，全部路线**（`routes.js`）。
   但要说清楚：**这不是那个问题的答案，是绕过了它。** 原问题是「两种模式渲染的短评/标签
   会不会不同」，而对比从没做过。走到今天，短评是量着抽出来的（movie 用 `span.comment`、
   音乐和剧用位置判据），也就是说 grid 这一路够用；至于 list 那边会不会多出点什么，
   仍然不知道。真要收掉这一条，得拿同一个账号的两种模式各抓一页做逐字段比对。
3. ~~**广播详情页要不要逐条抓？**~~ **v0.3 已解决**：tofu 证实时间线 API 在 **140 字**处截断并以 `...` 结尾，可精确检测（`text.length >= 140 && text.endsWith('...')`）。所以答案是「**只对被截断的补抓**」——既拿到全文，又不用为几万条全量补抓。HTML 面同理需确认是否也截断。
4. ~~**`intent` 词表的初始取值**~~ **已随 `bundle/1.2` 定稿**，在 `doubak-data-specs` 里。
   跨仓库一致性测试（`tools/generate-spec-constants.mjs --check`）守着它不漂移，CI 里
   显式验过它确实跑了、不是静默跳过。
5. ~~**哪些路线走 API、哪些走 HTML、哪些两边都走？**~~ **实现是全 HTML，一条 API 路线都没有。**
   这与上面写的倾向（用户创作内容两边都抓、目录数据只走 API）不一样，而且它是**默默发生
   的**——没有哪一次决定推翻过那个倾向，只是一条条路线都照 HTML 写下来了。

   事后看这个结果站得住，理由比当初的倾向更硬：HTML 是**用户当时真正看到的那一页**，
   而档案的承诺是「十几年后还能还原当时看到了什么」；API 返回的是同一份数据的另一种
   剪裁，字段多寡随豆瓣心情变。而且少一套鉴权（`ck`）就少一整类会在半夜全线 4xx 的
   失败模式。**但这是补记的理由，不是当时权衡过的。** 真要走 API，得先说清它能拿到
   HTML 拿不到的什么。
6. ~~**`ck` 令牌的生命周期。**~~ **没有兑现成问题。** `transport.js` 会在需要时带上它，
   而几小时的真实抓取（4 小时 16 分、5880 条捕获、零失败）里没有出现过中途全线 4xx。
   前提是第 5 条：全走 HTML，`ck` 的暴露面本来就小。哪天真去走 Rexxar API，这一条要
   重新问一遍。

## 7. 依赖 doubak-data-specs 的部分（必须先落 spec）

spec 是基石，下面这些**在 spec 定稿前不能写死**：

- `bundle/manifest.json` schema —— `spec_version`、`coverage`、`intent`，以及本次新增的**抓取存档信息**（§3.4）
- `checkpoint.json` schema（§3.5）
- `index.ndjson` 每行 schema
- `intent` 取值词表（开放词表，初始集合要定）
- verdict 枚举
- bundle 目录布局与文件命名规则（§2.1）

`canonical/` 现在不用管——那是 parser 的事，且应当等有了真实抓取数据再设计。

## 8. 里程碑

| 阶段 | 内容 | 出口标准 |
|---|---|---|
| **M0** | 技术验证 | ① WARC 写入（风险已降低：`CompressionStream('gzip')` 是原生的，WARC 记录本身就是文本头 + 载荷，不需要引入任何库，也就绕开了 `unsafe-eval`）② MV3 能否连续跑满 30 分钟不掉 ③ OPFS + File System Access 大文件读写是否可靠（含 `persist()` 获批率）④ ~~广播绝对时间~~ **已解决**（§3.3）⑤ ~~`statuses_count` 是否可信~~ **已定论：计数器不可信**（§3.1）。改为观测性任务：记录各路线 `claimed` 与 `captured` 的差值分布，看审查造成的偏差有多大 ⑥ **`declarativeNetRequest` 改写 Referer 是否可行**，以及 `ck` 令牌的轮换行为 ⑦ **`chrome.webRequest` 能否补齐响应头**（上文保真度问题）⑧ HTML 广播页的长正文是否也截断（API 在 140 字处截断，HTML 未知） |
| **M1** | 骨架 | 身份识别 + 覆盖率基线 + frontier + 抓一条路线 + 写出能被 ReplayWeb.page 打开的 bundle。**先用离线夹具开发（§10），真实抓取只在最后验证** |
| **M2** | 广播全线 | 时间边界 + 连续性证明 + 分类器 + awaiting_human + checkpoint 全流程 |
| **M3** | 其余路线 | 长文、标记列表、图片队列 |
| **M4** | 可发布 | 存储管理页、导出/导入、覆盖率对账 UI、增量抓取、隐私标注 |

M0 的第①和第④项是整个项目最大的两个未知数，应当最先做。M1 之前一行生产代码都不该写死 bundle 格式。

---

## 9. v0 抓哪些分类（已定）

前代的 `category.proto` 把豆瓣分类列成了一个枚举，其中一半标着 not supported。那份清单混了三个不同的轴（见 spec 的 `vocabularies/intent.json`），不能直接照搬。按**不可替代性**重排后的 v0 建议范围：

### v0 抓这些（已定）

| 分类 | 理由 |
|---|---|
| 个人主页 / 分类入口 | 声明数量的来源，必须最先抓 |
| **广播** | 发布后不可编辑、可静默删除、最不可替代、最时间敏感。**唯一真正紧急的东西** |
| **标记列表：book / movie / music / game / drama** | 五类。原计划六类（与前代 `category.proto` 一致），`app` 已于 2026-08-14 实测确认上游废弃，见下 |
| **评论（影评 / 书评等长评）** | 自己写的长文，无公开副本 |
| 日记、读书笔记 | 同上 |
| **用户上传图片 / 相册** | 不抓图正是「备份必须联网才能看」的病根，也是与 tofu 的核心差异 |
| 作品详情页 | 生成静态站的必要输入。受 F-03k 门控，排在广播之后 |

**`app`（移动应用）：明确不支持。2026-08-14 实测定论。**

上面那段原来写着「路线未知，需要在 M0/M1 实地发现」，并且预留了一句「也有可能这条路线已经不存在——那样就如实记录『豆瓣已无此入口』」。实地点过之后，就是那一种：

| 量到的 | |
|---|---|
| 标记列表页 | **打得开** |
| 作品详情页 | **全部 404** |

所以这不是「还没做」，是**上游的目录数据没有了**。抓下来只会得到一堆 404，而一条永远抓不到东西的路线比没有这条路线更糟：它会在覆盖率表上留一行永远为 0 的条目，看起来像 bug，然后每个新来的人都要重查一遍——而重查这件事要对着真实豆瓣点，是这个项目最贵的一种确认。

**但「不支持这个分类」不等于「你的 app 标记备份不了」，这一点必须说清。** 标记会生成广播，而广播是照抓的（app 类型条目的 `data-object-kind=3064`）。丢的只是那个豆瓣自己都已经不提供的作品页。这是与 F-04e-1「封面抽不到的两种情形」同一条原则：**上游本来就没有的东西，不能记成我们的缺口。**

留下的历史事实（对读老档案的人仍然有用）：前代的 `category.proto` 声明了 `app = 5` 但 `collector.go` 没实现；tofu 也没有这条路线；相关链接是 `douc.cc` 短链，真实 URL 藏在跳转后面；该账号只有 2 个不同的 app 对象。

**格式那边不动。** `intent.json` 与 canonical 的 `medium` 里 `app` 照留——bundle 词表是冻结的，而且老档案里可能真有 app 标记；读取方遇到不认识的取值必须原样保留。**爬虫不抓，与格式不认，是两件事。**

### v0 **不**抓

| 分类 | 状态 | 理由 |
|---|---|---|
| **豆邮**（私信） | **不支持** | 见 §9.1 |
| 豆列 | 延后 | 价值中等，可后补 |
| **关注 / 被关注 / 黑名单** | **永不** | 社交周边，理由见 §9.2 |
| **广播下的他人回应** | **永不** | 同上 |
| **小组、小站、同城活动、豆品** | **永不** | 不是「你的数据」，且体量无边界 |

### 9.1 豆邮：标为不支持

我此前把豆邮排在 v0 之后，理由是实现风险，并且说明「按不可替代性它本该紧跟广播」。**用户给出了一条我没有的信息，这条信息比我的理由更根本**：

> 豆邮里混着大量**站方群发的系统通知**，噪音极大。

这削弱的不是实现难度，而是**前提本身**。我原先假定豆邮 ≈ 私人通信 ≈ 极高不可替代性；实际上其中相当一部分根本不是个人往来，而是可替代的系统广播。真正不可替代的那部分被淹没在噪音里，而把噪音一并存进档案，还要连带承担存储、隐私和发布路径的风险。

**结论：标为不支持。** 若将来重启，先解决的应当是「区分个人往来与站方群发」这个问题——在那之前抓下来的东西，价值配不上它带来的风险。

### 9.2 社交周边一律不抓 —— 边界画不住

这条现在是**明确的范围决定**，不是「以后再说」。

**一致性论据（决定性的一条）**：既然不抓广播下的他人回应，就没有道理去抓关注列表——两者都是社交外围。只抓其中一个是自相矛盾的。

**边界论据**：任何一条社交边都是可以无限展开的入口。关注列表指向别的用户，那些用户又有自己的内容和关注列表；回应指向回应者，回应者又有自己的广播。而档案必须是**有界**的。

**隐私论据**：这些数据的主体不是本人，而是他人的身份与言论。对本项目的用户群，其中一部分涉及政治敏感，误入发布路径的伤害不可逆。

**实测补充（v0.5）**：广播列表页只带回应的**容器与计数**，不带回应**内容**——

```html
<div class="comments-items"></div>     <!-- 空的 -->
```

所以现状是「没有顺手抓到」，而不是「已经抓到了只是没标注」。要抓回应就得为每条广播额外发一次请求（实测账号 2839 条广播 = 2839 次额外请求），风控暴露面显著上升。

**如果将来仍要抓回应**，必须硬性限定：只抓自己内容下的回应、**深度为 1**、绝不跟随到回应者主页。没有这个边界就不要开始——兔子洞正是这么挖出来的。

### 9.3 为什么不需要「预留字段」

`category.proto` 里那种为未支持分类预留枚举号的做法，是 **protobuf 的线格式约束**：tag 号用过就不能重用，否则老解析器会误读。

JSON + 开放词表没有这个问题：新增分类就是多写一个字符串，老读者按规范原样保留即可。在这里搞预留反而有害——**它会让人以为这是个封闭集合，而它恰恰不是**（实测就冒出了前代词表里没有的 `loc`）。

该做的是另一件事：把**已知存在但暂不抓**的分类记录下来，让实现者知道还有什么在那儿。已写进 `vocabularies/intent.json` 的 `known_unsupported`。**记录 ≠ 预留。**

---

## 10. 用真实档案做离线夹具（v0.5 新增）

用户手上有一份真实旧档案（附录 C：2022-12 至 2024-08，782 MB，7353 个 HTML）。这对 v0 开发的价值比「有点样本」大得多——**它让绝大部分开发工作可以完全离线进行，一次豆瓣请求都不用发。**

### 10.1 为什么这件事很关键

这个项目最难测的东西，恰恰是**你不能主动制造的场景**：

| 场景 | 真实档案里有现成的 |
|---|---|
| 会话过期 | ✔ 2 个真实登录页（`20231218.1022/1027`） |
| 静默的零字节响应 | ✔ 7 个（同一次会话失效） |
| 被审查抑制的中段空洞 | ✔ 游戏列表第 7、14、17 页 |
| 头部插入造成的跨页重复 | ✔ 每批 7～11 条 |
| markup 跨年漂移 | ✔ `item` → `item comment-item` |
| 越界终止页 vs 封锁页（都是 0 条） | ✔ 两者俱全，正是分类器最该分对的一组 |
| 他人内容混入自己时间线 | ✔ 每批 21～27 条 |

**这些场景没法靠故意触发豆瓣风控来复现**——那正是这个项目要不惜代价避免的事。档案里已经有了，等于白捡。

### 10.2 怎么用

- **分类器（F-05）用它当回归集。** 每个文件配一个期望 verdict，跑一遍全量 7353 个文件。判错登录页 = 测试失败。
- **翻页与连续性逻辑（F-03e/f/g）离线跑。** 把档案挂成一个本地 fixture 服务器，让 frontier 真的走一遍分页，验证停滞检测、去重、水位线推进。
- **bundle 写入器（G）用它压体积。** 782 MB 是段轮转、OPFS 写入、导出续传的现成负载——不用等真人抓几小时。
- **选择器复验（附录 A/C.5）** 直接对着两个年代的页面跑。

### 10.3 一条必须遵守的约束

**这份档案是用户的个人数据，不能原样进公开仓库。** 里面有真实用户名、真实社交关系、真实观影记录，以及他人的身份信息。

因此：

- 夹具仓库**保持私有**，或只提交**脱敏后**的最小子集（改写用户名/ID、删除他人内容、只保留结构）。
- 公开仓库里的测试只放**合成夹具**——按真实档案的结构手工构造，但不含任何真人数据。
- 判定逻辑所需的那几个关键页面（登录页、零字节、空洞页）**脱敏后体积很小**，完全可以公开，这是性价比最高的一批。

---

## 附录 A：路线信息（源自前代实现，**部分已复验，部分已被推翻**）

> 取自 `its-my-data/doubak` 的 `task/collector.go`（master）。该项目已归档，豆瓣页面结构
> 确实变了。
>
> **2026-07 对真实豆瓣实跑之后的结论：**
>
> | 这里写的 | 实际 |
> |---|---|
> | 广播页标题含「广播」 | 已改成**「我的动态」**。别拿显示文字当结构标志（F-04k） |
> | 数字用户 ID 在广播条目的 `data-uid` 上 | 那**只在**广播条目上，且在作品详情页上是**评论者**的 ID。正确来源是全局导航（F-04j） |
> | 标记列表的条目链接是 `/subject/N` | 只对影视/图书/音乐成立。游戏是 `/game/N`、应用 `/app/N`、舞台剧 `/location/drama/N`（F-03o） |
> | URL 模板 | 已复验，广播与五类标记列表都能跑通 |
>
> 剩下没复验的：日记 / 影评 / 相册那几条（走 Rexxar 接口，见附录 B）。

### A.1 URL 模板

| 路线 | URL | 步长 |
|---|---|---|
| 个人主页 | `https://www.douban.com/people/<user>/` | — |
| 广播 | `https://www.douban.com/people/<user>/statuses?p=<n>` | 每页条数**不固定** |
| 电影 | `https://movie.douban.com/people/<user>/{collect,wish,do}?start=<n>&sort=time&rating=all&filter=all&mode=grid` | 15 |
| 书籍 | `https://book.douban.com/people/<user>/{collect,wish,do}?start=<n>&sort=time&rating=all&filter=all&mode=grid` | 15 |
| 音乐 | `https://music.douban.com/people/<user>/{collect,wish,do}?start=<n>&sort=time&rating=all&filter=all&mode=grid` | 15 |
| 游戏 | `https://www.douban.com/people/<user>/games?action={collect,wish,do}&start=<n>` | 15 |
| 舞台剧 | `https://www.douban.com/location/people/<user>/drama/{collect,wish}?sort=time&start=<n>&filter=all&mode=grid&tags_sort=count` | 15 |

`collect / wish / do` 对应 已完成 / 想要 / 进行中。舞台剧**没有 `do`**。`sort=time` 下 `start=0` 是最新，`start` 递增即新→旧，与 F-03e 一致。

### A.2 声明数量的取数位置（F-02a）

| 分类 | 页面 | 选择器 | 关键词 |
|---|---|---|---|
| 电影 | `movie.douban.com/people/<user>/` | `div#db-movie-mine > h2` | 看过 / 想看 / 在看 |
| 书籍 | `book.douban.com/people/<user>/` | `div#db-book-mine > div > h2` | 读过 / 想读 / 在读 |
| 音乐 | `music.douban.com/people/<user>/` | `div#db-music-mine > h2` | 听过 / 想听 / 在听 |
| 游戏 | `www.douban.com/people/<user>/games` | `div.article > div.tabs > a` | 玩过 / 想玩 / 在玩 |
| 舞台剧 | `www.douban.com/location/people/<user>/drama/` | `div.article > div.mod > h2` | 看过 / 想看 |

游戏与舞台剧没有独立入口页，总数在列表页的 tab 上（F-02e）。

### A.3 结构锚点（供 F-05b 分类器与链接抽取用）

| 用途 | 选择器 |
|---|---|
| 广播条目 | `div.status-wrapper > div.status-item` |
| 电影条目 | `div.item > div.info > ul > li.title > a:nth-child(1)` |
| 书籍条目 | `li.subject-item > div.info > h2 > a` |
| 游戏条目 | `div.common-item > div.content > div.title > a:nth-child(1)` |
| 音乐条目 | `div.item > div.info > ul > li.title > a:nth-child(1)` |
| 舞台剧条目 | `div.item > div.info > ul > li.title > a:nth-child(1)` |

### A.4 广播的 `data-target-type` 取值

前代在 `div.status-item` 上观察到的全部取值：

```
movie book music game      条目相关
sns                        自己发的短文（可带图）
app                        应用
ilmen                      「我喜欢」的东西（图 + 标题）
fav                        「我喜欢」的日记/帖子
olivia                     参与的讨论串
doulist                    豆列
rec                        推荐的讨论串
board                      已废弃：参与电影投票活动
""（空）                    转发
```

前代只统计不处理。这份词表可直接作为 `intent` / 内容类型的初始取值来源。**注意**：广播 URL 形如 `https://www.douban.com/people/<uid>/status/<status_id>/`，其中 `<uid>` 与本人不同即为他人内容——F-12a 的现成信号。

### A.5 前代的判定逻辑（几乎等于没有，仅作对照）

```go
// 全部的「封锁检测」：
if strings.Contains(*body, "<title>登录豆瓣</title>") { log.Fatal(...) }
// 全部的「不存在检测」：
if strings.Contains(t, "页面不存在") { /* Real user not found */ }
```

无速率限制器（`colly.LimitRule` 的 Delay 带着 `FIXME: this doesn't seem to do anything?`，实际靠手写 `time.Sleep(1s)`）、无重试、无 blocked/challenge 判定、无断点续抓。这两个字符串仍可作为 `login` 与 `gone` 的信号纳入 F-05b，但它们只是分类器的一小部分。

---

## 附录 B：tofu / 豆伴（`doufen-org/tofu`）

> MV3 扩展，JavaScript，620 star，维护至 2025-03。目前最接近的同类实现，**唯一一个真正做出来的浏览器端豆瓣备份工具**。
> 定位差异：tofu 备份的是**结构化数据**（存 IndexedDB，导出 Excel）；doubak 备份的是**页面本身**（WARC）。所以它的路线知识可以全盘吸收，它的存储/归档策略则正是我们要改的地方。

### B.1 Rexxar 移动端 API（最大的收获）

全部形如 `https://m.douban.com/rexxar/api/v2/...`，需要 `ck` 查询参数 + 正确的 Referer：

| 路线 | 端点 |
|---|---|
| 广播时间线 | `/status/user_timeline/{uid}?max_id={maxId}&ck={ck}&for_mobile=1` |
| 单条广播全文 | `/status/{id}?ck={ck}&for_mobile=1` |
| 标记（含总数） | `/user/{uid}/interests?type={type}&status={status}&start={start}&count={count}&ck={ck}&for_mobile=1` |
| **标记总数（金丝雀）** | `/user/{uid}/interests?ck={ck}&count=1&for_mobile=1` → `json.total` |
| 日记 | `/user/{uid}/notes?start={start}&count=50&ck={ck}&for_mobile=1` |
| 影评/书评 | `/user/{uid}/reviews?type={type}&start={start}&count=50&ck={ck}&for_mobile=1` |
| 读书笔记 | `/user/{uid}/annotations?start={start}&count=50&ck={ck}&for_mobile=1` |
| 相册 | `/user/{uid}/photo_albums?start={start}&count=50&ck={ck}&for_mobile=1` |
| 豆列 | `/user/{uid}/{type}_doulists?start={start}&count=50&ck={ck}&for_mobile=1` |
| 关注 | `/user/{uid}/following?start={start}&count=50&ck={ck}&for_mobile=1` |

`interests` 的 `type` 取值：`game / music / book / movie / drama`。分页 `count=50`（比 HTML 的 15 高 3 倍多，请求数少得多）。

**广播用游标 `max_id` 而非偏移**——这正是 F-03f「按值续抓」想要的东西，且天生免疫头部插入错位。

### B.2 广播总数

`status.js` 里 `this.total = this.targetUser.statuses_count;`——用户对象上带着广播总数。**这与「广播没有可靠总数」的前提冲突，M0 必须实测**（§3.1）。tofu 只拿它当进度条分母，没做对账，所以它自己也没验证过这个数准不准。

### B.3 我们原本漏掉的路线

`extension/tasks/` 下的完整清单：

```
status      广播              note        日记
interest    标记（五类）       review      影评/书评
annotation  读书笔记 ★         doulist     豆列
photo       相册/照片          doumail     豆邮（私信）★★
following   关注              follower    被关注
blacklist   黑名单            board       (旧版看板)
files       图片抽取
```

★ **读书笔记（annotation）**：用户创作内容，我们完全漏了，必须补进高优先级。
★★ **豆邮（doumail）**：最不可替代（无公开副本）且隐私最敏感（含对方的话）。注意它**不走 Rexxar**，走旧版 `https://www.douban.com/doumail/?start={start}` 与 `https://www.douban.com/j/doumail/loadmore`。见 F-03j。

`following / follower / blacklist` 属于社交图谱，按 v1 范围（「对单个用户是否有价值」）应当抓下来存档，但**不进任何发布路径**。

### B.4 必须照抄的技术手法

**Referer 改写**：`fetch()` 不允许设 `Referer`。tofu 的做法是发一个自定义头 `X-Override-Referer`，再用 `declarativeNetRequest` 规则把它改写成真正的 `Referer`：

```js
fetch(url, {headers: {'X-Override-Referer': 'https://m.douban.com/mine/statuses'}})
```

manifest 里因此需要 `declarativeNetRequest` + `declarativeNetRequestWithHostAccess` 权限，以及 `cookies`（用于读 `ck`）。

**`ck` 令牌**：从 cookie 读出来，当查询参数拼进每个 API URL。会话相关，轮换行为待测（§6.6）。

**长文截断**：时间线 API 在 140 字截断：

```js
if (status.text.length >= 140 && status.text.substr(-3, 3) === '...') {
    item.status = await this.fetchStatusFulltext(id);
}
```

### B.5 值得借鉴的：版本戳与字段级比对

tofu 的 IndexedDB schema 里每张表都有 `version`，值取自 `jobId`（一次同步 = 一个版本号），配合复合索引 `[type+status+version]`。变更检测靠 `compareInterest()` 逐字段比 status / comment / rating / tags，变了就把旧值塞进 `row.history[lastVersion]`。

这是我们 canonical 修订模型的一个轻量前身，验证了两件事：
1. **逐字段比对确实必要**——整体对象比对会把改评分和改评论混为一谈（我们的 F-07h 是它的精细化版本：加 NFC 规范化、逐字段 digest）。
2. **「删除重建会换掉记录指纹」是真事**——tofu 显式处理了「同一 subject 但 interest.id 变了」的情况（`delete` 旧行再插新行并标记 changed）。CLAUDE.md 里关于 `recorded_at` 是指纹而非时钟的判断，在这里得到实证。

它缺的是：跨版本的 `parser_version` 概念、NFC 规范化、以及指向原始捕获的回溯链路——这三样正是 doubak 的增量。

### B.6 我们要明确避开的

| tofu 的做法 | 问题 | 我们的对策 |
|---|---|---|
| **图片上传到 Cloudinary**（`api.cloudinary.com/v1_1/{cloud}/image/upload`），本地只存 URL | 备份要么依赖豆瓣在线、要么依赖第三方图床在线。**这正是 CLAUDE.md 说的「tofu 的备份需要豆瓣在线」的根源** | F-04e：图片字节直接进 WARC，零外部依赖 |
| manifest 未声明 `unlimitedStorage` | 大体量备份会撞配额 | F-08a/b |
| 任务状态存 `chrome.storage.session` | **浏览器一重启，队列全没** | F-10b/c：IndexedDB + checkpoint |
| 遇重复 ID 即判定「追上了」并结束（单个 `retried` 标志兜底） | 删除、乱序、整页重复都会导致**提前终止且不报错** | F-03g 停滞检测 |
| 只判 `response.status !== 200` 就抛错 | 豆瓣用 200 送封锁页，**完全测不出来** | F-05a~c |
| 固定 1 秒间隔，无抖动、无自适应退避 | 节奏机械，且软封锁后不会降速 | F-04b |
| 网络错误重试 2 次，不区分错误类型 | 在软封锁上重试 = 往封号方向推 | F-06a |
| 支持备份**他人**账号（`isOtherUser`） | 超出 v1 范围，且隐私风险大 | 不做 |

### B.7 结论

tofu 把「怎么从豆瓣把数据取出来」这件事解决得比前代好得多，路线覆盖也全得多——这部分应当全盘吸收。它没有解决的是「取出来之后怎么保证十年后还打得开」：没有原始页面、没有出处、没有覆盖率证明、图片托管在第三方。**这正是 doubak 的差异化所在**，与 CLAUDE.md 里「archival layer nobody else builds」的判断一致。

---

## 附录 C：真实旧档案的实测发现（v0.4）

> 数据来源：用户用前代工具 `its-my-data/doubak` 抓下的真实档案，2022-12 至 2024-08 共 20 个批次、7353 个 HTML 文件、782 MB，单一账号。
> 这是本设计第一次拿到**真实豆瓣页面**来验证假设。以下结论均可复现。

### C.1 证实了的设计判断

| 判断 | 实测 |
|---|---|
| 广播每条都有绝对时间 | ✔ `class="created_at" title="2024-05-12 14:43:19"`，秒级，每条都有 |
| 头部插入会产生跨页重复（F-03e） | ✔ 每次全量抓取有 7～11 条跨页重复。**是重复，不是遗漏**——正是设计所要的方向 |
| 广播每页条数不固定（F-03g） | ✔ 实测 20 / 21 / 22 条不等，末页 8 条，越界页 0 条 |
| 自己的时间线里混有他人内容（F-12a） | ✔ 每批 21～27 条的 `data-uid` 不是本人 |
| 响应体积可作分类信号（F-05b/e） | ✔ 广播正常页中位数 98 KB，登录页 16199 字节，越界终止页 19240 字节，区分明显 |

**广播条目的属性比我们以为的丰富**，可直接用于去重与第三方标注：

```html
<div class="status-item"
     data-sid="4606440067"        ← 广播 ID，去重锚点
     data-uid="82160871"          ← 作者；≠ 本人即为他人内容（F-12a 的现成信号）
     data-action="1"
     data-target-type="movie"
     data-object-kind="1002"
     data-object-id="36221195">
```

另外发现一个前代词表里没有的 `data-target-type` 取值：**`loc`**（4 次，同城相关）。前代会对它打 WARNING 然后忽略。这印证了 `intent` / 内容类型必须是**开放词表**，且未知取值必须原样保留而非丢弃。

### C.2 审查导致计数不可信 —— 拿到了直接证据

用户指出豆瓣计数不可信。这份档案里能**直接看到**：

| 路线 | 声称 | 实际渲染 | 差值 |
|---|---|---|---|
| 电影／看过 | 1157 | 1157 | **0** |
| 游戏／玩过 | 293 | 288 | **−5** |

同一次抓取、同一时刻、同一工具。而且游戏的缺口**在列表中段而非末页**：

```
游戏/玩过 各页条数：[15,15,15,15,15,15,14,15,15,15,15,15,15,14,15,15,13,15,15,7]
                                    ↑第7页          ↑第14页    ↑第17页
电影/看过 各页条数：[15,15,15, …… ,15,2]     ← 除末页外全部 15
```

槽位是 15，中段却只渲染出 14、14、13 条——这正是**条目被计入总数、却在渲染时被抑制**的指纹。该偏差可复现且随时间增长：−3（2022-12）→ −3（2023-01）→ −5（2023-12）→ −5（2024-08）。不是抓取抖动。

**关键推论：你无法在事先不知道答案的情况下判断哪个分类的计数可信。** 拿它当完整性闸门会同时犯两个错——把电影**误判为完整**（它同样可能有被抑制的条目，只是看不见），并让游戏**永远重试**。

由此产生三条新规则，已写进 spec：

- **短页 ≠ 末页。** 前代的 `if itemCount != pageStep { 可能是最后一页 }` 会在第 7、14、17 页误判。终止必须靠停滞检测。
- **不得用声称数量决定翻多少页。** 前代 `for start < claimed_count` 在 `claimed < 实际` 时会静默截断整个列表。
- 声称数量照记（事后不可恢复，且有取证价值），但**不下完整性结论**。

### C.3 静默数据损坏 —— 三种，都真实发生了

**一、登录页被当成数据写进了档案。**

批次 `20231218.1022` 与 `20231218.1027` 各有一个文件名为 `..._broadcast_p1.html` 的文件，内容其实是**登录页**（`<title>登录豆瓣</title>`，含验证码，16199 字节）——会话过期了，工具照样按数据文件名写进磁盘。

前代确实有 `FailIfNeedLogin` 检查，但它在 `saveResponse` **之后**才执行：文件已经落盘，然后进程 `log.Fatal` 退出，磁盘上留下一个没有任何标记的毒文件。

要命的地方在于：**这两个文件的条目数是 0，而同一批次里合法的「翻过最后一页」终止页条目数也是 0。** 在文件层面二者**无法区分**，只能靠体积和标题。

> 这是 `verdict` 必须逐条捕获、且必须进 spec 第一版的最强论据。存下封锁页是对的（F-05d），但**存了不标就是最坏的组合**。

**二、7 个零字节文件。**

`items/` 下有 7 个**零字节** HTML 文件，全部来自 `20231218.1205`——与上面那次会话失效同一天。它们文件名齐全地躺在目录里，没有任何东西表明抓取失败过。下游工具只会看到「文件在」。

> 已加入 spec §6.5.2 与校验器：**载荷零长度而 verdict 为 ok，必须报错。**
>
> 扩展这边另有一条与路线无关的规则（2026-07-30 补）：**0 字节的 HTTP 200 一律判
> `null`**。我们不知道它为什么空，而「判不出来」是唯一诚实的答案。

**三、25 个「页面不存在」被当成作品页存了下来**（2026-07-30 补）。

`items/` 下有 25 个 17 KB 左右的文件，`<title>页面不存在</title>`——条目被删了或改了 ID，
而前代照样以作品页的文件名写进磁盘。同样没有任何标记。

现在它们会被判成 `soft404`。这一条与前两条是同一个病：**「HTTP 200 即成功」**。

### C.4 markup 漂移是真的

电影列表页的条目容器：

| 抓取时间 | class |
|---|---|
| 2022-12-26、2023-01-27 | `class="item"` |
| 2023-12-18、2024-08-11 | `class="item comment-item"` |

**两年之内、单个用户的档案里就变了一次。** 写死选择器的解析器会在这里静默返回零条目。这正是 `canonical/` 推迟到有真实数据之后再设计的理由，也是「捕获与解释必须分开」的实证。

### C.5 对附录 A 的更正

前代记录的选择器有两处需要修正：

- **声称数量在每一张列表页上都有**，不只在分类入口页：`<h1>我看过的影视(1157)</h1>`。比原设想的好——可以逐页复读，从而发现抓取过程中总数发生了变化。
- 电影条目的实际 class 是 `item comment-item`（2023-12 起）。CSS 选择器 `div.item` 两个时期都能命中，但**精确字符串匹配会漏**。分类器锚点必须按「class 包含」匹配，不能按等值匹配。

### C.6 增量抓取能省多少

前代每次都全量重抓。这份档案里：

| 批次 | 广播页数 | 唯一条目 | 相比上次新增 |
|---|---|---|---|
| 2022-12-25 | 116 | 2299 | — |
| 2023-12-18 | 132 | 2620 | 321 条（约 16 页） |
| 2024-08-11 | 143 | 2839 | 219 条（约 11 页） |

2024 那次抓了 **143 页**才拿到约 **11 页**的新内容——**92% 是重复劳动**，而且每一次多余的请求都在增加账号被风控的暴露面。

另外：三次抓取里，**最早一条的时间戳完全一致**（`2014-01-10 22:56:49`），且 2022 年见过的条目在 2024 年**一条没少**（0 条删除）。说明这份历史是稳定的，水位线机制有坚实的锚点。

### C.7 分类器实跑真实档案的发现（v0.6）

> **这一节的数字后来被 C.10 修正了。** 下面统计的是**列表页与广播页**（1012 个）。
> 算上作品详情页（6341 个）之后，未登录的比例远不止这里看到的——见 C.10。
> 结论（「必须判 login 而不是 ok」）不变，只是问题的规模比当时以为的大得多。

把分类器（F-05）对着那 1012 个列表/广播页跑了一遍：

```
ok      861
login   151
```

**151 个「login」全部是真阳性。** 有整整几个批次是在**未登录状态下**抓的：

| 批次 | 分类 | 登录 |
|---|---|---|
| 20230127 | 电影（105 页）、游戏（36 页） | **否** |
| 20230204 / 20231218 | 舞台剧 | **否** |
| 20231218 | 音乐（6 页） | **否** |

豆瓣的公开列表**对匿名访问者照常显示**，所以前代工具照样拿到了数据、照样存了盘，没有任何标记。这比登录页那两个文件更隐蔽——那两个至少一眼能看出是登录页，而这些页面里有真实的条目数据，看起来完全正常。

**为什么必须判为 `login` 而不是 `ok`**：公开视图里**没有私密条目**。把它当成这个账号的数据存进档案，等于悄悄地把一份不完整的列表冒充成完整的。

### C.8 顺带复核了审查那个结论（结论不变）

发现有批次未登录之后，需要排除一个可能：游戏那个 −5 的差值会不会只是「未登录看不到私密条目」造成的？

按登录状态分开重算：

| 类别 | 已登录批次的差值 | 未登录批次的差值 |
|---|---|---|
| 电影 / 书 / 音乐 / 舞台剧 | **全部 0** | **全部 0** |
| 游戏 · 玩过 | −3（2022-12）、−5（2023-12）、−5（2024-08） | −3（2023-01） |
| 游戏 · 在玩 | −1（三个批次） | −1 |

**登录状态不解释这个差值**：已登录的批次照样差，未登录的批次差得一样多。差值只出现在游戏这一类，且跨四个批次稳定复现。审查／屏蔽的解释仍然成立，spec §5.3 的实测证据不用改。

（顺带修正一处我自己的测量失误：用一个合并的正则去数所有分类的条目，会在游戏页上多数出 20 个，得出 +15 这种荒谬的正值。每个分类必须用自己的容器 class。）

### C.9 浏览器实测：持久化存储与配额（v0.6）

在 Chrome 151 / ChromeOS 上跑自检页（`selftest/`）的结果：

| 项 | 结果 |
|---|---|
| OPFS / CompressionStream / crypto.subtle | 全部可用 |
| File System Access | 可用（导出可行） |
| 配额 | 61.15 GB 可用（真实档案约 0.8 GB，绰绰有余） |
| FileStore 契约、bundle 写入、崩溃恢复 | **28 项全通过** |
| `navigator.storage.persist()` | **返回 false** |

**M0 spike ③ 就此关闭**，但 `persist()` 那一项需要说清楚，因为我一开始把它读错了。

#### `persist()` 返回 false 是预期行为，不是风险信号

扩展防驱逐靠的是 manifest 里的 `unlimitedStorage` 权限，**不是**这个 API；Chrome
也从不为持久化存储弹窗询问，所以对扩展而言 `persist()` 基本恒为 false。文档建议
调用它，但那条建议对扩展是误导性的。

自检页原本把这个 false 展示成「被拒绝——档案可能被浏览器清掉」，那是**我写错了**，
已改正。

#### 但也不能当作绝对安全

文档说 `unlimitedStorage` 免于驱逐，社区里却有「装了这个权限、数据照样被清掉」的
报告。这不是能靠一个权限就闭眼的事。

**结论不是去申请更强的许可，而是不要让 OPFS 成为唯一的一份。** bundle 是追加式
分段的，所以：

> **段一封就可以导出，不必等整场抓完。**

这样 OPFS 里始终只压着当前这一段，而不是四小时的全部成果。界面上要显示「还有多少
没导出」并给出导出入口（docs/ui.md §6.1）——那是**可执行的动作**，比展示一个用户
看不懂的 `persist(): false` 有用得多。

这条改动的来源值得记一笔：如果不是真机实测，`persist()` 那个 false 要么被忽略、
要么被当成致命问题，两个方向都会做出错误的设计。

#### 吞吐：45.5 MB/s，存储不是瓶颈，也不要去优化它

实测 64 MB 连续写入用时 1407 ms。据此：

| | |
|---|---|
| 真实档案体量 | 约 800 MB |
| 写盘总耗时 | **约 18 秒** |
| 一场真实抓取 | 数千次请求 × 数秒节奏 = **数小时** |

即使把每次 append 的 5.5 ms 全算成开销而非字节传输（7300 条记录 ≈ 40 秒），占比
仍在千分之一量级。

**因此「缓存 sync access handle」这个优化点已关闭，不是推迟。** 缓存的代价是实打实
的：同一文件同时只能有一个 handle，缓存就要管理生命周期，还要保证异常路径上一定
释放——而崩溃恢复恰恰是异常路径最密集的地方。为千分之一的收益引入这种复杂度是净亏。

留一个开着的 TODO 会招来「顺手优化一下」，所以直接写成结论并附上数据。

### C.10 作品详情页：6341 页实测（2026-07-30 新增）

拿新的判定描述跑一遍旧档案里全部 6341 个作品详情页，结果全部**正确**：

| 判定 | 数量 | 是什么 |
|---|---|---|
| `ok` | 4190（66.1%） | 正常页面 |
| `login` | **2119（33.4%）** | **前代在未登录状态下抓的** |
| `soft404` | 25（0.4%） | 标题「页面不存在」 |
| `null` | 7（0.1%） | **0 字节的响应**，被前代当数据留在了磁盘上 |

两条修正：

1. **附录 C.7 的「151 个未登录页面」低估了。** 那个数只统计了列表页。算上作品详情页，
   旧档案里**三分之一的目录数据是匿名视图**，而且几乎全在一天之内（2023-01-27 一天
   2036 页）。匿名视图缺少本人的标记与评分，且未登录状态的频率上限更低——那一天既在
   写不完整的数据，也在提高被限流的概率。
2. **0 字节的 200 也曾被当成数据。** 7 个，同一天。当时的判定逻辑就是「HTTP 200 即
   成功」。现在空响应一律判 `null`（与路线无关）：我们不知道它为什么空，而「判不出来」
   是唯一诚实的答案。

### C.11 作品页的框架标志必须是「至少中一个」

量标志的时候撞到一张关键页面：**2017 年央视春晚**（`movie/26729145`）——88 KB 的完全
正常的页面，但豆瓣**关掉了它的评分**，所以没有 `id="interest_sectl"`。

第一版把那个当成必须存在的标志，于是这类页面会被判成「认不出来」并整场停机。对一个
专门在意审查痕迹的项目来说，把「评分被关掉」当成故障尤其荒谬：那正是最该完整存下来的
东西。

所以作品详情页用 `anyFrameAnchors`（至少中一个），而不是列表页那种「缺一不可」。
「任一」仍然足够严——封锁页与错误页一个区块都不会有，而 `urlAnchor` 已经保证了资源
正确，这里只需回答「内容渲染出来了吗」。

各媒介的覆盖（已排除空文件、soft404、未登录页，全部 100%）：

| 标志 | movie | book | music | game | drama |
|---|---|---|---|---|---|
| `id="interest_sectl"` | ✔ | ✔ | ✔ | ✔ | ✗ |
| `id="mainpic"` | ✔ | ✔ | ✔ | ✗ | ✗ |
| `<div id="info"` | ✔ | ✔ | ✔ | ✗ | ✗ |
| `id="comments"` | ✗ | ✗ | ✗ | ✔ | ✗ |
| `drama-info` | ✗ | ✗ | ✗ | ✗ | ✔ |

四个媒介各有至少两条独立标志，任一条被改掉都还剩一条。**舞台剧只有一条且未校准**：
旧档案里 3 个舞台剧详情页全部是未登录时抓的，手上没有任何登录态样本。
