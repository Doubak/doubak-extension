# 图标

**当前这四个 PNG 是占位图**，由 `tools/make-icons.py` 用纯 zlib 生成（这个项目
零依赖，不引绘图库）：深绿底 + 两颗白豆。够用来让 `chrome.notifications` 有个
`iconUrl`、让扩展在工具栏里不是一片空白，**不是最终设计**。

正式图标要满足：

- 16 / 32 / 48 / 128 四个尺寸，PNG
- 16px 下仍然认得出来（那是标签页与工具栏的实际显示尺寸）
- 深浅两种系统主题下都看得清
- 不使用豆瓣的商标或配色到会引起混淆的程度——这是第三方工具，不是豆瓣官方产品
